{
    "name": "atoms/example-laravel",
    "description": "A minimal Laravel application with one persistent Atom, ready to deploy to Cloudflare.",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": "^8.3",
        "atoms/laravel": "^0.4",
        "laravel/framework": "^11.0 || ^12.0"
    },
    "require-dev": {
        "atoms/cli": "^0.4",
        "atoms/phpstan-rules": "^0.4",
        "atoms/testing": "^0.4",
        "orchestra/testbench": "^9.0 || ^10.0",
        "phpunit/phpunit": "^11.5"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "post-autoload-dump": [
            "Illuminate\\Foundation\\ComposerScripts::postAutoloadDump",
            "@php artisan package:discover --ansi"
        ],
        "post-root-package-install": [
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\""
        ]
    },
    "config": {
        "allow-plugins": {
            "php-http/discovery": true
        },
        "sort-packages": true
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
