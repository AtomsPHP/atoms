/**
 * Auto-generated bundle by build-bundle.mjs
 * Do not edit manually.
 */

export default {
  manifest: {
  "bundle_format": 0,
  "abi": {
    "php": "8.3"
  },
  "atoms": {
    "Counter": {
      "class": "App\\Atoms\\Counter",
      "file": "/app/Counter.php",
      "migrations": [
        "/app/migrations/001_init.sql",
        "/app/migrations/002_add_stats.sql"
      ]
    },
    "Vault": {
      "class": "App\\Atoms\\Vault",
      "file": "/app/Vault.php",
      "migrations": [
        "/app/migrations/vault/001_init.sql",
        "/app/migrations/vault/002_ledger.sql"
      ]
    }
  }
},
  files: {
  "/app/Counter.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace App\\Atoms;\n\nuse Atoms\\Atom;\n\n/**\n * Counter — a fixture Atom for conformance testing.\n *\n * Demonstrates:\n * - SQL updates and queries\n * - in-memory state across turns within a residency\n * - lifecycle hooks\n * - array serialization\n */\nfinal class Counter extends Atom\n{\n    /**\n     * Calibration for spin() below: iterations between clock reads, and the\n     * work budget per requested millisecond. Fixture test data, not\n     * operational capacity values — nothing the platform does is tuned by\n     * them, and the conformance check they serve passes at any value that\n     * keeps the turn well inside the Durable Object's CPU limit.\n     */\n    private const SPIN_CHUNK = 4096;\n    private const SPIN_ITERATIONS_PER_MS = 60000;\n\n    private int $turnsThisResidency = 0;\n\n    /**\n     * Increment the counter by the given amount and return the new value.\n     */\n    public function increment(int $by): int\n    {\n        $this->turnsThisResidency++;\n\n        $this->db()->execute(\n            'UPDATE counter_state SET value = value + ? WHERE id = ?',\n            [$by, 1]\n        );\n\n        $rows = $this->db()->query('SELECT value FROM counter_state WHERE id = ?', [1]);\n        return (int) ($rows[0]['value'] ?? 0);\n    }\n\n    /**\n     * Slowly increment the counter, so conformance check 11 can prove that two\n     * concurrent invokes of one Atom serialize.\n     *\n     * The delay is deliberately *not* driven by the clock alone. On deployed\n     * workerd the wall clock does not advance inside a synchronous run —\n     * Cloudflare freezes time between I/O operations — so the obvious\n     * `while (hrtime(true) - $start < $target)` spins forever in production:\n     * measured 2026-08-04, every `slowIncrement` ran until the Durable Object\n     * hit its CPU limit (\"Durable Object exceeded its CPU time limit and was\n     * reset\", cpuTime 30000ms) and the request became a 500, at any $delayMs,\n     * while the same loop passed under `wrangler dev`. The loop is therefore\n     * bounded by a work budget as well; the clock test is kept so it still\n     * exits on time wherever time does advance.\n     *\n     * Serialization itself does not depend on how long this takes: a turn is\n     * one synchronous run of the guest and the Durable Object holds a turn\n     * mutex. The budget only has to keep the first turn running while the\n     * second request arrives, and stay far below the CPU limit.\n     */\n    public function slowIncrement(int $by, int $delayMs): int\n    {\n        $this->turnsThisResidency++;\n\n        $this->spin($delayMs);\n\n        $this->db()->execute(\n            'UPDATE counter_state SET value = value + ? WHERE id = ?',\n            [$by, 1]\n        );\n\n        $rows = $this->db()->query('SELECT value FROM counter_state WHERE id = ?', [1]);\n        return (int) ($rows[0]['value'] ?? 0);\n    }\n\n    /**\n     * Get the current counter value.\n     */\n    public function getValue(): int\n    {\n        $this->turnsThisResidency++;\n\n        return $this->readValue();\n    }\n\n    /**\n     * Get statistics as an array (exercises Serializer arrays).\n     *\n     * `turnsThisResidency` counts every turn this residency has served,\n     * including this one — it is the conformance suite's observable for warm\n     * residency (checks 3 and 12), so every invocable method must bump it.\n     */\n    public function getStats(): array\n    {\n        $this->turnsThisResidency++;\n\n        return [\n            'turnsThisResidency' => $this->turnsThisResidency,\n            'currentValue' => $this->readValue(),\n            'activations' => $this->readActivations(),\n            'timestamp' => time(),\n        ];\n    }\n\n    /**\n     * How many times onActivation() has run against this Atom's durable state.\n     * Proves the lifecycle hook fired, and that it fires again after eviction.\n     */\n    public function getActivations(): int\n    {\n        $this->turnsThisResidency++;\n\n        return $this->readActivations();\n    }\n\n    /**\n     * Throw an uncaught exception, so the turn fails the way a real customer\n     * bug would (conformance check 8: `atom_exception`, residency survives).\n     */\n    public function boom(string $message): void\n    {\n        $this->turnsThisResidency++;\n\n        throw new \\RuntimeException($message);\n    }\n\n    /**\n     * Try to read a host-owned table. The JS bridge rejects it before it\n     * reaches the Durable Object's SQLite (conformance check 10); the rejection\n     * arrives here as a \\PDOException, which this method reports rather than\n     * rethrows so the check can assert on it.\n     *\n     * @return array{rejected: bool, message: string}\n     */\n    public function readReserved(): array\n    {\n        $this->turnsThisResidency++;\n\n        try {\n            $this->db()->query('SELECT value FROM __atoms_meta WHERE key = ?', ['atom_type']);\n        } catch (\\PDOException $e) {\n            return ['rejected' => true, 'message' => $e->getMessage()];\n        }\n\n        return ['rejected' => false, 'message' => ''];\n    }\n\n    /**\n     * The same rejection, reached through SQL that a lexical guard is easy to\n     * get wrong on: an apostrophe inside a `--` comment, followed by a second\n     * statement (`sql.exec` runs them all). A scanner that pairs quotes without\n     * knowing about comments desynchronises from SQLite's tokenizer here and\n     * blanks the `UPDATE` out of the text it checks, while SQLite skips the\n     * comment and runs it — writing `__atoms_meta.atom_type` and 409-ing every\n     * future activation of this Atom.\n     *\n     * @return array{rejected: bool, message: string}\n     */\n    public function readReservedViaComment(): array\n    {\n        $this->turnsThisResidency++;\n\n        try {\n            $this->db()->execute(\n                \"SELECT 1; -- it's fine\\nUPDATE __atoms_meta SET value = 'Bogus' WHERE key = 'atom_type'\"\n            );\n        } catch (\\PDOException $e) {\n            return ['rejected' => true, 'message' => $e->getMessage()];\n        }\n\n        return ['rejected' => false, 'message' => ''];\n    }\n\n    /**\n     * Report what the guest clock does across pure computation and across a\n     * host round trip. Kept in the fixture because it is the only way to see\n     * the frozen-clock behaviour that `slowIncrement` is written around, and\n     * because the answer is a property of the deployed platform rather than of\n     * this code — a future runtime change is visible here immediately.\n     *\n     * @return array{iterations: int, spinNs: int, sqlNs: int, checksum: int}\n     */\n    public function clockProbe(int $iterations): array\n    {\n        $this->turnsThisResidency++;\n\n        $t0 = hrtime(true);\n        $checksum = 0;\n        for ($i = 0; $i < $iterations; $i++) {\n            $checksum += $i;\n        }\n        $t1 = hrtime(true);\n        $this->readValue();\n        $t2 = hrtime(true);\n\n        return [\n            'iterations' => $iterations,\n            'spinNs' => $t1 - $t0,\n            'sqlNs' => $t2 - $t1,\n            'checksum' => $checksum,\n        ];\n    }\n\n    /**\n     * Burn a bounded amount of work, aiming at $delayMs but never trusting the\n     * clock to end the loop (see slowIncrement()). The clock is read once per\n     * chunk rather than once per iteration so that a frozen clock costs a\n     * predictable amount of work and a live clock still ends the spin promptly.\n     */\n    private function spin(int $delayMs): void\n    {\n        if ($delayMs <= 0) {\n            return;\n        }\n\n        $startNs = hrtime(true);\n        $targetNs = $delayMs * 1_000_000;\n        $budget = $delayMs * self::SPIN_ITERATIONS_PER_MS;\n\n        for ($done = 0; $done < $budget; $done += self::SPIN_CHUNK) {\n            $checksum = 0;\n            for ($i = 0; $i < self::SPIN_CHUNK; $i++) {\n                $checksum += $i;\n            }\n            if ((hrtime(true) - $startNs) >= $targetNs) {\n                return;\n            }\n        }\n    }\n\n    /** Read the durable counter without counting a turn. */\n    private function readValue(): int\n    {\n        $rows = $this->db()->query('SELECT value FROM counter_state WHERE id = ?', [1]);\n\n        return (int) ($rows[0]['value'] ?? 0);\n    }\n\n    /** Rows written by onActivation(), one per residency. */\n    private function readActivations(): int\n    {\n        $rows = $this->db()->query('SELECT count(*) AS n FROM counter_activations');\n\n        return (int) ($rows[0]['n'] ?? 0);\n    }\n\n    /**\n     * Lifecycle hook: called on activation.\n     */\n    protected function onActivation(): void\n    {\n        $this->db()->execute(\n            'INSERT INTO counter_activations (atom_id, activated_at) VALUES (?, ?)',\n            [$this->id, date('Y-m-d H:i:s')]\n        );\n    }\n}\n",
  "/app/Vault.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace App\\Atoms;\n\nuse Atoms\\Atom;\n\n/**\n * Vault — a fixture Atom for int64 and transaction testing.\n *\n * Demonstrates:\n * - int64 boundary cases through arguments, SQL, and results\n * - db()->transaction() with rollback\n * - db()->pdo() direct access\n */\nfinal class Vault extends Atom\n{\n    /**\n     * Store a big (potentially int64) value by key.\n     */\n    public function putBig(string $key, int $value): void\n    {\n        $this->db()->execute(\n            'INSERT OR REPLACE INTO vault_data (key, value) VALUES (?, ?)',\n            [$key, $value]\n        );\n    }\n\n    /**\n     * Retrieve a big value by key.\n     *\n     * The CAST is load-bearing, not cosmetic. MEASURED (wrangler 4.118,\n     * 2026-08-04): `ctx.storage.sql` hands an INTEGER wider than 2^53-1 to JS\n     * as a double, so the exact value is already gone before the bridge sees\n     * it — `SELECT 9223372036854775807` comes back as 9223372036854775808.\n     * The host refuses such a value with a typed `int64_precision` error rather\n     * than returning a quietly wrong integer, so a wide integer must leave\n     * SQLite as TEXT. SQLite renders it exactly; PHP's (int) cast on the\n     * decimal string is lossless on this 64-bit build.\n     */\n    public function getBig(string $key): int\n    {\n        $rows = $this->db()->query(\n            'SELECT CAST(value AS TEXT) AS value FROM vault_data WHERE key = ?',\n            [$key]\n        );\n        return (int) ($rows[0]['value'] ?? 0);\n    }\n\n    /**\n     * Write inside a transaction and read the same row back from inside that\n     * same transaction, returning what the *uncommitted* read saw\n     * (conformance check 6: read-your-own-write).\n     */\n    public function putAndReadInTransaction(string $key, int $value): int\n    {\n        return $this->db()->transaction(function ($db) use ($key, $value) {\n            $db->execute(\n                'INSERT OR REPLACE INTO vault_data (key, value) VALUES (?, ?)',\n                [$key, $value]\n            );\n\n            $rows = $db->query(\n                'SELECT CAST(value AS TEXT) AS value FROM vault_data WHERE key = ?',\n                [$key]\n            );\n\n            return (int) ($rows[0]['value'] ?? 0);\n        });\n    }\n\n    /**\n     * Write inside a transaction, read the write back from inside that same\n     * transaction (so it is genuinely observed), then throw — which must make\n     * the host discard the write set (conformance check 7).\n     *\n     * @return array{observed: int, rolledBack: bool}\n     */\n    public function putReadThenFail(string $key, int $value): array\n    {\n        $observed = null;\n\n        try {\n            $this->db()->transaction(function ($db) use ($key, $value, &$observed) {\n                $db->execute(\n                    'INSERT OR REPLACE INTO vault_data (key, value) VALUES (?, ?)',\n                    [$key, $value]\n                );\n\n                $rows = $db->query(\n                    'SELECT CAST(value AS TEXT) AS value FROM vault_data WHERE key = ?',\n                    [$key]\n                );\n\n                $observed = (int) ($rows[0]['value'] ?? 0);\n\n                throw new \\RuntimeException('Deliberate failure after an observed write.');\n            });\n        } catch (\\RuntimeException $e) {\n            return ['observed' => (int) $observed, 'rolledBack' => true];\n        }\n\n        return ['observed' => (int) $observed, 'rolledBack' => false];\n    }\n\n    /**\n     * Append a ledger row through `db()->pdo()` and return the rowid PDO\n     * reports, which crosses the bridge as `last_insert_rowid`\n     * (conformance check 9's lastInsertId leg, and a second AtomsPDO exercise).\n     */\n    public function appendLedger(string $key, int $value): int\n    {\n        $pdo = $this->db()->pdo();\n\n        $stmt = $pdo->prepare('INSERT INTO vault_ledger (key, value) VALUES (?, ?)');\n        $stmt->execute([$key, $value]);\n\n        return (int) $pdo->lastInsertId();\n    }\n\n    /**\n     * Append a ledger row, then do other work, then ask PDO for the rowid.\n     *\n     * Regression cover: `appendLedger()` reads `lastInsertId()` with nothing in\n     * between, which hid a bug where the host reported `last_insert_rowid: 0`\n     * for every statement that wrote no rows — so any read (or an UPDATE that\n     * matched nothing) between the INSERT and the read of the id silently reset\n     * it to 0. Real PDO/SQLite keeps it until the next successful insert.\n     *\n     * @return array{immediate: int, afterRead: int}\n     */\n    public function appendLedgerThenRead(string $key, int $value): array\n    {\n        $pdo = $this->db()->pdo();\n\n        $stmt = $pdo->prepare('INSERT INTO vault_ledger (key, value) VALUES (?, ?)');\n        $stmt->execute([$key, $value]);\n        $immediate = (int) $pdo->lastInsertId();\n\n        // A plain read, and a write that matches no row: both report\n        // rows_written = 0, and neither may disturb the reported rowid.\n        $this->db()->query('SELECT count(*) AS n FROM vault_ledger');\n        $this->db()->execute(\n            'UPDATE vault_data SET value = value WHERE key = ?',\n            ['no-such-key-' . $key]\n        );\n\n        return ['immediate' => $immediate, 'afterRead' => (int) $pdo->lastInsertId()];\n    }\n\n    /**\n     * Open a transaction through the documented `db()->pdo()` surface, write,\n     * and return without committing — the shape of an ordinary application bug\n     * (a forgotten `commit()`, or an exception caught above this frame).\n     *\n     * The turn must come back as `atom_exception` with the write discarded, and\n     * the Atom must keep serving turns. It used to strand the guest at a park\n     * the host refuses while a transaction is open, which destroyed the\n     * residency and, because the failure is deterministic, destroyed it again\n     * on every retry.\n     */\n    public function leakTransaction(string $key, int $value): string\n    {\n        $pdo = $this->db()->pdo();\n        $pdo->beginTransaction();\n\n        $this->db()->execute(\n            'INSERT OR REPLACE INTO vault_data (key, value) VALUES (?, ?)',\n            [$key, $value]\n        );\n\n        return 'returned with the transaction still open';\n    }\n\n    /**\n     * Return a value nested past what `json_encode()` will carry, so the turn\n     * boundary itself fails. That must be a typed turn error, not a\n     * \\RuntimeException thrown out of the parked loop (which would unwind\n     * php.run() and poison the residency).\n     *\n     * @return array<int|string, mixed>\n     */\n    public function returnDeeplyNested(int $depth): array\n    {\n        $node = ['leaf' => true];\n\n        for ($i = 0; $i < $depth; $i++) {\n            $node = [$node];\n        }\n\n        return $node;\n    }\n\n    /**\n     * Read a ledger row back by rowid, exact for wide integers (see getBig()).\n     *\n     * @return array{id: int, key: string, value: int}|null\n     */\n    public function readLedger(int $id): ?array\n    {\n        $rows = $this->db()->query(\n            'SELECT id, key, CAST(value AS TEXT) AS value FROM vault_ledger WHERE id = ?',\n            [$id]\n        );\n\n        if ($rows === []) {\n            return null;\n        }\n\n        return [\n            'id' => (int) $rows[0]['id'],\n            'key' => (string) $rows[0]['key'],\n            'value' => (int) $rows[0]['value'],\n        ];\n    }\n\n    /**\n     * Transfer a value between two keys using db()->transaction().\n     * Demonstrates rollback by forcing a failure after a write.\n     */\n    public function transfer(string $fromKey, string $toKey, int $amount, bool $shouldFail = false): bool\n    {\n        try {\n            $this->db()->transaction(function ($db) use ($fromKey, $toKey, $amount, $shouldFail) {\n                // Deduct from source\n                $db->execute(\n                    'UPDATE vault_data SET value = value - ? WHERE key = ?',\n                    [$amount, $fromKey]\n                );\n\n                // Add to destination\n                $db->execute(\n                    'UPDATE vault_data SET value = value + ? WHERE key = ?',\n                    [$amount, $toKey]\n                );\n\n                // Force failure if requested (to test rollback)\n                if ($shouldFail) {\n                    throw new \\RuntimeException('Simulated transfer failure for rollback testing');\n                }\n            });\n            return true;\n        } catch (\\RuntimeException $e) {\n            // Transaction was rolled back\n            return false;\n        }\n    }\n\n    /**\n     * Method using db()->pdo() directly.\n     * Demonstrates that the PDO shim works for customer code.\n     */\n    public function queryWithPdo(string $key): array\n    {\n        $pdo = $this->db()->pdo();\n        $stmt = $pdo->prepare('SELECT key, value FROM vault_data WHERE key = ? LIMIT 1');\n        $stmt->execute([$key]);\n        $result = $stmt->fetchAll(\\PDO::FETCH_ASSOC);\n        return $result;\n    }\n\n    /**\n     * Lifecycle hook: ensure tables are populated with test data.\n     */\n    protected function onActivation(): void\n    {\n        // Initialize with test values if they don't exist\n        $this->db()->execute(\n            'INSERT OR IGNORE INTO vault_data (key, value) VALUES (?, ?)',\n            ['balance_a', 1000000]\n        );\n        $this->db()->execute(\n            'INSERT OR IGNORE INTO vault_data (key, value) VALUES (?, ?)',\n            ['balance_b', 2000000]\n        );\n    }\n}\n",
  "/app/migrations/001_init.sql": "CREATE TABLE counter_state (\n    id INTEGER PRIMARY KEY,\n    value INTEGER NOT NULL DEFAULT 0\n);\n\nINSERT INTO counter_state (id, value) VALUES (1, 0);\n\nCREATE TABLE counter_activations (\n    id INTEGER PRIMARY KEY AUTOINCREMENT,\n    atom_id TEXT NOT NULL,\n    activated_at TEXT NOT NULL\n);\n",
  "/app/migrations/002_add_stats.sql": "CREATE TABLE counter_stats (\n    id INTEGER PRIMARY KEY,\n    stat_key TEXT NOT NULL UNIQUE,\n    stat_value TEXT,\n    recorded_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP\n);\n",
  "/app/migrations/vault/001_init.sql": "CREATE TABLE vault_data (\n    key TEXT PRIMARY KEY,\n    value INTEGER NOT NULL\n);\n",
  "/app/migrations/vault/002_ledger.sql": "CREATE TABLE vault_ledger (\n    id INTEGER PRIMARY KEY AUTOINCREMENT,\n    key TEXT NOT NULL,\n    value INTEGER NOT NULL\n);\n",
  "/atoms/core/resources/errors.json": "{\n    \"$comment\": \"Single source of truth for the ATOMS-E* error catalog. Append-only; never renumber. Ranges: docs/conventions.md. docsUrl anchors are stable.\",\n    \"errors\": [\n        {\n            \"code\": \"ATOMS-E001\",\n            \"title\": \"Unclassifiable file under the Atoms path\",\n            \"message\": \"File {file} under the Atoms path is neither an Atom, Methods, AtomJob, Shared DTO, nor a migration.\",\n            \"fix\": \"Move non-Atoms code out of the Atoms path, or make the class extend the appropriate atoms/core base class.\",\n            \"severity\": \"warning\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E010\",\n            \"title\": \"Framework symbol referenced inside Atom code\",\n            \"message\": \"{symbol} belongs to a web framework and cannot be used inside Atom code — the Atoms runtime has no framework loaded.\",\n            \"fix\": \"Move the behavior into a Methods class (runs in your app via $this->app()) or use the atoms/core equivalent.\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E011\",\n            \"title\": \"Global framework helper called inside Atom code\",\n            \"message\": \"The helper {symbol}() does not exist in the Atoms runtime.\",\n            \"fix\": \"Use $this->config() for configuration, new \\\\DateTimeImmutable() for time, or move the call into a Methods class.\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E012\",\n            \"title\": \"Monolith class referenced inside Atom code\",\n            \"message\": \"Reference to monolith class {symbol} inside Atom code. Atom code ships to the platform; your application's classes do not.\",\n            \"fix\": \"Pass data across the boundary as a Shared DTO (implement Atoms\\\\Serialization\\\\Payload) or call it via $this->app().\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E013\",\n            \"title\": \"Package not declared in atoms-composer.json\",\n            \"message\": \"Atom code references {symbol} from package {package}, which is not listed in atoms-composer.json.\",\n            \"fix\": \"Add the package to atoms-composer.json if it is on the approved list; otherwise remove the dependency.\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E014\",\n            \"title\": \"Facade used inside Atom code\",\n            \"message\": \"Facade {symbol} cannot be used inside Atom code — there is no service container in the Atoms runtime.\",\n            \"fix\": \"Move the behavior into a Methods class and call it via $this->app().\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E015\",\n            \"title\": \"Shared class references a non-core symbol\",\n            \"message\": \"Shared class {class} references {symbol}. Shared code runs in both worlds and may only use atoms/core and the PHP stdlib.\",\n            \"fix\": \"Keep Shared classes pure data: promoted scalar/Payload properties, accessors and factories only.\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E016\",\n            \"title\": \"Shared class contains behavior\",\n            \"message\": \"Shared class {class} method {method} looks like behavior (I/O or side effects), not data.\",\n            \"fix\": \"Shared classes are DTOs. Move behavior to the Atom (World A) or a Methods class (World B).\",\n            \"severity\": \"warning\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E017\",\n            \"title\": \"env() called inside Atom code\",\n            \"message\": \"Atoms cannot read your application's .env.\",\n            \"fix\": \"Use `atoms secrets set KEY --env <environment>` and read it with $this->config('KEY').\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E018\",\n            \"title\": \"Native PHP serialization at a boundary\",\n            \"message\": \"serialize()/unserialize() must not appear in Atom, Shared, or boundary code.\",\n            \"fix\": \"Boundary values cross as JSON through the Atoms serializer; implement Atoms\\\\Serialization\\\\Payload for structured data.\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E019\",\n            \"title\": \"PHP extension not available in the runtime image\",\n            \"message\": \"Atom code uses extension {extension}, which the Atoms runtime image does not provide.\",\n            \"fix\": \"Check the supported extension list in the docs, or move the dependent code into a Methods class.\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E020\",\n            \"title\": \"Boundary type outside the serialization algebra\",\n            \"message\": \"{location} uses type {type}, which cannot cross the Atoms boundary.\",\n            \"fix\": \"Allowed: scalars, null, arrays of allowed types, Payload DTOs, DateTimeImmutable, backed enums.\",\n            \"severity\": \"error\",\n            \"phase\": \"static-analysis\"\n        },\n        {\n            \"code\": \"ATOMS-E021\",\n            \"title\": \"ORM object crossing the boundary\",\n            \"message\": \"{type} is an ORM object (Eloquent model / Doctrine entity) and cannot cross the Atoms boundary.\",\n            \"fix\": \"Pass a Shared DTO instead, e.g. PlayerSnapshot::fromUser($user) or $user->only([...]).\",\n            \"severity\": \"error\",\n            \"phase\": \"static-analysis\"\n        },\n        {\n            \"code\": \"ATOMS-E022\",\n            \"title\": \"Unserializable value in a boundary payload\",\n            \"message\": \"A {type} cannot cross the Atoms boundary.\",\n            \"fix\": \"Closures, resources, and container-bound objects cannot be serialized. Pass plain data or a Payload DTO.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E023\",\n            \"title\": \"Payload class is not hydratable\",\n            \"message\": \"Payload class {class} must declare all state as promoted constructor properties with allowed types.\",\n            \"fix\": \"Use public readonly promoted constructor properties; remove non-promoted state.\",\n            \"severity\": \"error\",\n            \"phase\": \"static-analysis\"\n        },\n        {\n            \"code\": \"ATOMS-E024\",\n            \"title\": \"Value does not match the declared boundary type\",\n            \"message\": \"Cannot denormalize {value} into {type} at {location}.\",\n            \"fix\": \"The wire value and the declared PHP type disagree — check the manifest for version skew (atoms diff).\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E030\",\n            \"title\": \"Unknown Methods method called from an Atom\",\n            \"message\": \"{atom} calls $this->app()->{method}(), but {methodsClass} has no public method {method}.\",\n            \"fix\": \"Add the method to the Methods class, or fix the call — method names are checked statically against the manifest.\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E031\",\n            \"title\": \"Methods call-site signature mismatch\",\n            \"message\": \"{atom} calls {methodsClass}::{method}() with arguments that do not match its signature.\",\n            \"fix\": \"Update the call site or the Methods signature; both sides are part of the deployed contract.\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E032\",\n            \"title\": \"AtomJob constructor signature mismatch\",\n            \"message\": \"{atom} dispatches new {job}(...) with arguments that do not match its constructor.\",\n            \"fix\": \"AtomJob constructor parameters are the dispatch contract — keep promoted properties and the dispatch site in sync.\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E033\",\n            \"title\": \"Dispatched class is not an AtomJob\",\n            \"message\": \"{atom} dispatches {class}, which does not extend Atoms\\\\AtomJob.\",\n            \"fix\": \"Extend Atoms\\\\AtomJob, or run the code inline in the Atom instead of dispatching it.\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E040\",\n            \"title\": \"Manifest hash mismatch (version skew)\",\n            \"message\": \"The monolith was built against manifest {local} but the platform is serving {deployed}.\",\n            \"fix\": \"Deploy order matters: additive Atom changes deploy Atoms-first; contractions deploy monolith-first. Run `atoms diff`.\",\n            \"severity\": \"warning\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E041\",\n            \"title\": \"Method not in deployed version\",\n            \"message\": \"Method {method} on {type} does not exist in the currently deployed manifest ({version}).\",\n            \"fix\": \"The monolith is ahead of the Atom deploy. Deploy Atoms first for additive changes (`atoms deploy`), then the monolith.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E042\",\n            \"title\": \"Bundle rejected by platform validation\",\n            \"message\": \"The platform build service rejected the bundle: {reason}.\",\n            \"fix\": \"Run `atoms validate` locally — it applies the same checks and must reproduce the failure.\",\n            \"severity\": \"error\",\n            \"phase\": \"deploy\"\n        },\n        {\n            \"code\": \"ATOMS-E043\",\n            \"title\": \"atoms/core version outside the runtime's supported range\",\n            \"message\": \"Bundle was built against atoms/core {built}, but the platform runtime supports {supported}.\",\n            \"fix\": \"composer update atoms/core to a supported version and rebuild.\",\n            \"severity\": \"error\",\n            \"phase\": \"deploy\"\n        },\n        {\n            \"code\": \"ATOMS-E050\",\n            \"title\": \"Shipped migration was edited\",\n            \"message\": \"Migration {file} differs from the version recorded in the deployed manifest.\",\n            \"fix\": \"Migrations are append-only once shipped. Restore the original file and add a new migration instead.\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E051\",\n            \"title\": \"Migration numbering conflict\",\n            \"message\": \"Migrations for {type} have a duplicate or non-increasing version at {file}.\",\n            \"fix\": \"Number migrations with strictly increasing NNN_ prefixes (001_, 002_, ...).\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E052\",\n            \"title\": \"Migration likely exceeds the activation budget\",\n            \"message\": \"Migration {file} looks heavier than the activation-time budget ({budget_ms}ms).\",\n            \"fix\": \"Migrations run at Atom activation. Keep them tiny, or restructure as lazy per-row upgrades in Atom code.\",\n            \"severity\": \"warning\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E053\",\n            \"title\": \"Migration failed at activation\",\n            \"message\": \"Migration {version} for {type}/{id} failed: {reason}.\",\n            \"fix\": \"The Atom stays deactivated. Fix the migration by appending a corrective one — never edit the failed file — and redeploy.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E060\",\n            \"title\": \"Atom type not deployed\",\n            \"message\": \"Atom type {type} is not present in the customer's current deploy manifest.\",\n            \"fix\": \"Deploy the Atom (`atoms deploy`) or check the class basename — types are matched by basename.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E061\",\n            \"title\": \"Turn deadline exceeded\",\n            \"message\": \"The Atom's turn exceeded the configured deadline.\",\n            \"fix\": \"Turns must stay short. Move slow work to an AtomJob via $this->dispatch(), and check for accidental blocking I/O.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E062\",\n            \"title\": \"Capacity refused / rate limited\",\n            \"message\": \"The platform refused the request: {reason}. Retryable after backoff.\",\n            \"fix\": \"The SDK retries automatically where safe. Persistent refusals mean the fleet is at capacity — check the dashboard.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E063\",\n            \"title\": \"Remote Atom exception\",\n            \"message\": \"{class} thrown inside {type}/{id}::{method}: {message}\",\n            \"fix\": \"This is your Atom code throwing. The sanitized remote trace is attached to the exception.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E064\",\n            \"title\": \"Callback signature verification failed\",\n            \"message\": \"Rejected a platform callback: bad or missing Ed25519 signature.\",\n            \"fix\": \"Check the configured platform public key. If keys were rotated, update config and retry.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E065\",\n            \"title\": \"Callback replay detected\",\n            \"message\": \"Rejected a platform callback: stale timestamp or reused nonce.\",\n            \"fix\": \"Usually clock skew (>300s) on the app host, or a genuine replay. Sync clocks; investigate repeated nonces.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E066\",\n            \"title\": \"No Methods class for callback target\",\n            \"message\": \"A callback targeted {atomType}, but no Methods class resolves for it.\",\n            \"fix\": \"Create {expectedClass} (or mark a class with #[MethodsFor({atomType}::class)]).\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E070\",\n            \"title\": \"atoms.json missing or invalid\",\n            \"message\": \"Could not read atoms.json at the repository root: {reason}.\",\n            \"fix\": \"Run `atoms init` to create it, or fix the reported JSON error.\",\n            \"severity\": \"error\",\n            \"phase\": \"cli\"\n        },\n        {\n            \"code\": \"ATOMS-E071\",\n            \"title\": \"atoms-composer.json invalid or package not allowed\",\n            \"message\": \"{reason}\",\n            \"fix\": \"atoms-composer.json may only contain `require` (from the approved package list) and `repositories`.\",\n            \"severity\": \"error\",\n            \"phase\": \"cli\"\n        },\n        {\n            \"code\": \"ATOMS-E072\",\n            \"title\": \"Deploy credentials missing\",\n            \"message\": \"No Cloudflare API token found for environment {environment}.\",\n            \"fix\": \"Export CLOUDFLARE_API_TOKEN. It is never accepted as a command-line option — a credential in argv is visible to every process on the machine. In CI, supply it to the deploy action as `cloudflare-api-token`.\",\n            \"severity\": \"error\",\n            \"phase\": \"cli\"\n        },\n        {\n            \"code\": \"ATOMS-E073\",\n            \"title\": \"Wrangler not found\",\n            \"message\": \"Could not find a Wrangler executable for environment {environment}: {reason}.\",\n            \"fix\": \"Run `npm install` in the Worker directory so node_modules/.bin/wrangler exists, or set ATOMS_WRANGLER_BIN to an absolute path. Atoms never downloads Wrangler for you.\",\n            \"severity\": \"error\",\n            \"phase\": \"cli\"\n        },\n        {\n            \"code\": \"ATOMS-E074\",\n            \"title\": \"Wrangler command failed\",\n            \"message\": \"`wrangler {command}` exited with status {status}.\",\n            \"fix\": \"Read Wrangler's own output above; it reports Cloudflare API rejections verbatim. Check that the API token has Workers Scripts:Edit on the target account.\",\n            \"severity\": \"error\",\n            \"phase\": \"cli\"\n        },\n        {\n            \"code\": \"ATOMS-E075\",\n            \"title\": \"Cloudflare account not configured\",\n            \"message\": \"Environment {environment} has no Cloudflare account id.\",\n            \"fix\": \"Add \\\"account_id\\\" to that environment in atoms.json, or set CLOUDFLARE_ACCOUNT_ID. Find it on the Workers & Pages overview in the Cloudflare dashboard.\",\n            \"severity\": \"error\",\n            \"phase\": \"cli\"\n        },\n        {\n            \"code\": \"ATOMS-E076\",\n            \"title\": \"Worker directory missing or incomplete\",\n            \"message\": \"The Atoms Worker project for environment {environment} is not usable: {reason}.\",\n            \"fix\": \"Point \\\"worker_dir\\\" in atoms.json at a checkout of the Atoms Cloudflare Worker (cloudflare/worker), and run `npm ci` inside it.\",\n            \"severity\": \"error\",\n            \"phase\": \"cli\"\n        },\n        {\n            \"code\": \"ATOMS-E077\",\n            \"title\": \"Secret would not be readable from Atom code\",\n            \"message\": \"Refusing to store {key} as the Worker variable {name}: {reason}.\",\n            \"fix\": \"Pick a different key. $this->config() resolves through the Worker's ATOMS_CONFIG_ENV_PREFIX allowlist, and a name outside it — or on its deny list — reads back as null with no error.\",\n            \"severity\": \"error\",\n            \"phase\": \"cli\"\n        },\n        {\n            \"code\": \"ATOMS-E078\",\n            \"title\": \"Bundle path too long for the archive format\",\n            \"message\": \"{path} is {bytes} bytes and cannot be stored in the bundle archive.\",\n            \"fix\": \"The bundle is a ustar tar: a path needs a \\\"/\\\" positioned so the tail is at most 100 bytes and the head at most 155. Shorten the class or directory name.\",\n            \"severity\": \"error\",\n            \"phase\": \"build\"\n        },\n        {\n            \"code\": \"ATOMS-E080\",\n            \"title\": \"Callback channel not configured\",\n            \"message\": \"$this->app()/dispatch() requires the Worker's callback channel, which is not configured.\",\n            \"fix\": \"Set ATOMS_CALLBACK_URL on the Worker to your monolith's callback endpoint (the route where Atoms\\\\Client\\\\Callback\\\\CallbackKernel is mounted), and ATOMS_CALLBACK_SIGNING_KEY to the base64 of a 32-byte Ed25519 seed. `atoms dev --callback-url` sets the first for local runs.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E081\",\n            \"title\": \"Callback signing key missing or unusable\",\n            \"message\": \"ATOMS_CALLBACK_URL is set but the Worker has no usable Ed25519 signing key.\",\n            \"fix\": \"ATOMS_CALLBACK_URL is set but the Worker has no usable signing key, and Atoms never sends an unsigned callback. Set ATOMS_CALLBACK_SIGNING_KEY (wrangler secret put) to the base64 of a 32-byte Ed25519 seed, and give the matching public key to Ed25519Verifier in your app.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E082\",\n            \"title\": \"app() called inside a database transaction\",\n            \"message\": \"$this->app()->{method}() cannot be called inside $this->db()->transaction(): the transaction holds a synchronous Durable Object callback that cannot wait for an HTTP round trip.\",\n            \"fix\": \"Read what you need from the monolith before opening the transaction, or close the transaction and re-open it after the call. $this->dispatch() is allowed inside a transaction — it is queued on commit and dropped on rollback.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E083\",\n            \"title\": \"Callback request to the monolith failed\",\n            \"message\": \"Callback to the monolith for {atom}::{method} failed: {reason}.\",\n            \"fix\": \"The Atom reached your app but the call did not succeed. Check that the callback route is mounted and reachable from Cloudflare, that the Methods class exists for this Atom type, and that ATOMS_CALLBACK_TIMEOUT_MS is long enough for the endpoint.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E084\",\n            \"title\": \"Dispatched job cannot be encoded from its promoted properties\",\n            \"message\": \"{class} cannot be dispatched: {reason}.\",\n            \"fix\": \"An AtomJob's constructor parameters are its dispatch contract and must be promoted public properties. Declare public function __construct(public readonly int $orderId) — non-promoted state cannot cross the boundary.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E085\",\n            \"title\": \"Invalid timer name\",\n            \"message\": \"Timer name {name} is invalid: it must be a non-empty string within the runtime's byte limit.\",\n            \"fix\": \"Pick a name for $this->timers()->schedule($name, $at) that is non-empty and within the runtime's byte limit for timer names.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        },\n        {\n            \"code\": \"ATOMS-E086\",\n            \"title\": \"Timer limit exceeded\",\n            \"message\": \"{type}/{id} already has {count} scheduled timers, at the per-Atom limit.\",\n            \"fix\": \"Raise the per-Atom cap on scheduled timers (ATOMS_TIMERS_MAX on the Worker), or cancel/coalesce timers you no longer need before scheduling new ones.\",\n            \"severity\": \"error\",\n            \"phase\": \"runtime\"\n        }\n    ]\n}\n",
  "/atoms/core/src/Atom.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms;\n\nuse Atoms\\Runtime\\AtomContext;\nuse Atoms\\Timers\\Timers;\nuse Atoms\\Websocket\\Connection;\nuse Atoms\\Websocket\\Message;\n\n/**\n * Base class for every Atom. This is the entire World A runtime surface: what an\n * Atom class is allowed to see. Everything here is frozen ABI — a change to a\n * signature is a breaking change to every deployed customer bundle.\n *\n * Subclasses receive an {@see AtomContext} from the runtime and reach the\n * platform only through the protected accessors below.\n *\n * @template TApp of AtomMethods\n */\nabstract class Atom\n{\n    public readonly string $id;\n\n    private readonly AtomContext $context;\n\n    final public function __construct(string $id, AtomContext $context)\n    {\n        $this->id = $id;\n        $this->context = $context;\n    }\n\n    protected function db(): Database\n    {\n        return $this->context->db();\n    }\n\n    /**\n     * The reverse-RPC proxy into this Atom's Methods class (World B).\n     *\n     * @return TApp\n     */\n    protected function app(): object\n    {\n        return $this->context->app();\n    }\n\n    protected function dispatch(AtomJob $job): void\n    {\n        $this->context->dispatch($job);\n    }\n\n    protected function config(string $key): mixed\n    {\n        return $this->context->config($key);\n    }\n\n    /**\n     * @param array<string, mixed> $payload\n     */\n    protected function broadcast(string $channel, array $payload): void\n    {\n        $this->context->broadcast($channel, $payload);\n    }\n\n    protected function timers(): Timers\n    {\n        return $this->context->timers();\n    }\n\n    // Lifecycle hooks — invoked by the runtime via Atoms\\Runtime\\LifecycleInvoker.\n\n    protected function onActivation(): void\n    {\n    }\n\n    protected function onDeactivation(): void\n    {\n    }\n\n    /**\n     * Invoked by the runtime via {@see \\Atoms\\Runtime\\LifecycleInvoker} when a\n     * scheduled timer fires.\n     */\n    protected function onTimer(string $name): void\n    {\n    }\n\n    // WebSocket handlers — optional overrides.\n\n    /**\n     * @param array<string, string> $params\n     */\n    public function onConnect(Connection $conn, array $params): void\n    {\n    }\n\n    public function onMessage(Connection $conn, Message $msg): void\n    {\n    }\n\n    public function onDisconnect(Connection $conn): void\n    {\n    }\n}\n",
  "/atoms/core/src/AtomJob.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms;\n\n/**\n * World B base class for dispatched jobs. An Atom dispatches a job with\n * {@see Atom::dispatch()}; the job's `handle()` runs in the monolith.\n *\n * The constructor parameters ARE the dispatch contract: they must be declared as\n * promoted public properties whose types are drawn from the serialization\n * algebra (scalars, null, arrays of allowed types, {@see Serialization\\Payload}\n * DTOs, \\DateTimeImmutable, \\BackedEnum). The job is serialized on dispatch as\n * `{\"job\": \"FQCN\", \"args\": {\"param\": value, ...}}` and rehydrated by\n * constructor-parameter name on the monolith side. Non-promoted state cannot\n * survive the boundary.\n */\nabstract class AtomJob\n{\n}\n",
  "/atoms/core/src/AtomMethods.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms;\n\n/**\n * World B base class. A Methods class holds callback methods that run inside the\n * customer's monolith with full framework access, reached from an Atom via\n * {@see Atom::app()} (reverse RPC). This marker base carries no behavior — the\n * toolchain classifies subclasses by extension.\n */\nabstract class AtomMethods\n{\n}\n",
  "/atoms/core/src/Attributes/MethodsFor.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Attributes;\n\nuse Attribute;\n\n/**\n * Overrides Methods-class resolution: `#[MethodsFor(GameRoom::class)]` on a\n * class declares it as the Methods class for the given Atom type, replacing the\n * namespace-convention default.\n */\n#[Attribute(Attribute::TARGET_CLASS)]\nfinal class MethodsFor\n{\n    public function __construct(\n        public readonly string $atomClass,\n    ) {\n    }\n}\n",
  "/atoms/core/src/Attributes/SharedWithAtoms.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Attributes;\n\nuse Attribute;\n\n/**\n * Marks a DTO that lives outside the `Shared/` directory as boundary-shared, so\n * the toolchain applies Shared-zone purity rules to it wherever it sits.\n */\n#[Attribute(Attribute::TARGET_CLASS)]\nfinal class SharedWithAtoms\n{\n}\n",
  "/atoms/core/src/Database.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms;\n\n/**\n * The guaranteed database surface an Atom can touch. Backed per-Atom by a single\n * SQLite file in the runtime; {@see Sqlite\\SqliteDatabase} is the core\n * implementation. Query-builder ergonomics are an optional bridge on top — the\n * ABI is just this PDO-level interface.\n */\ninterface Database\n{\n    public function pdo(): \\PDO;\n\n    /**\n     * Run a SELECT (or other row-returning) statement.\n     *\n     * @param array<int|string, mixed> $bindings\n     * @return list<array<string, mixed>> the result rows as associative arrays\n     */\n    public function query(string $sql, array $bindings = []): array;\n\n    /**\n     * Run a writing statement (INSERT/UPDATE/DELETE/DDL).\n     *\n     * @param array<int|string, mixed> $bindings\n     * @return int the number of affected rows\n     */\n    public function execute(string $sql, array $bindings = []): int;\n\n    /**\n     * Run $fn inside a transaction, committing on success and rolling back if it\n     * throws. Returns whatever $fn returns.\n     *\n     * @param callable(Database): mixed $fn\n     */\n    public function transaction(callable $fn): mixed;\n}\n",
  "/atoms/core/src/Errors/AtomsError.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Errors;\n\n/**\n * Base exception for every Atoms failure. Carries a stable ErrorCode so callers\n * (and the agent tooling) can branch on the machine-readable code rather than\n * the human message.\n */\nclass AtomsError extends \\RuntimeException\n{\n    public readonly ErrorCode $errorCode;\n\n    public function __construct(ErrorCode $code, string $message = '', ?\\Throwable $previous = null)\n    {\n        $this->errorCode = $code;\n\n        if ($message === '') {\n            $message = ErrorCatalog::get($code)->title;\n        }\n\n        parent::__construct($message, 0, $previous);\n    }\n}\n",
  "/atoms/core/src/Errors/CatalogEntry.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Errors;\n\n/**\n * Immutable value object describing one entry in the error catalog.\n *\n * @phpstan-type Severity 'error'|'warning'\n */\nfinal class CatalogEntry\n{\n    /**\n     * @param 'error'|'warning' $severity\n     */\n    public function __construct(\n        public readonly ErrorCode $code,\n        public readonly string $title,\n        public readonly string $message,\n        public readonly string $fix,\n        public readonly string $docsUrl,\n        public readonly string $severity,\n        public readonly string $phase,\n    ) {\n    }\n}\n",
  "/atoms/core/src/Errors/ErrorCatalog.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Errors;\n\n/**\n * Loads and caches packages/core/resources/errors.json — the single source of\n * truth for the ATOMS-E### catalog — and renders canonical error messages.\n */\nfinal class ErrorCatalog\n{\n    private const CATALOG_PATH = __DIR__ . '/../../resources/errors.json';\n\n    private const DOCS_BASE = 'https://docs.atoms.cloud/errors#';\n\n    /** @var array<string, CatalogEntry>|null keyed by code value */\n    private static ?array $entries = null;\n\n    private function __construct()\n    {\n    }\n\n    public static function get(ErrorCode $code): CatalogEntry\n    {\n        $entries = self::load();\n\n        if (!isset($entries[$code->value])) {\n            throw new \\RuntimeException(\"No catalog entry for error code {$code->value}.\");\n        }\n\n        return $entries[$code->value];\n    }\n\n    /**\n     * @return array<string, CatalogEntry> keyed by code value (e.g. \"ATOMS-E012\")\n     */\n    public static function all(): array\n    {\n        return self::load();\n    }\n\n    /**\n     * Canonical rendering used by every package: the code, the message with\n     * {placeholders} substituted from $context, and the catalog fix line.\n     *\n     * @param array<string, scalar|\\Stringable> $context\n     */\n    public static function format(ErrorCode $code, array $context = []): string\n    {\n        $entry = self::get($code);\n\n        $message = $entry->message;\n        foreach ($context as $key => $value) {\n            $message = str_replace('{' . $key . '}', (string) $value, $message);\n        }\n\n        return sprintf('%s: %s Fix: %s', $entry->code->value, $message, $entry->fix);\n    }\n\n    /**\n     * @return array<string, CatalogEntry>\n     */\n    private static function load(): array\n    {\n        if (self::$entries !== null) {\n            return self::$entries;\n        }\n\n        $raw = @file_get_contents(self::CATALOG_PATH);\n        if ($raw === false) {\n            throw new \\RuntimeException('Could not read error catalog at ' . self::CATALOG_PATH);\n        }\n\n        /** @var array{errors: list<array{code: string, title: string, message: string, fix: string, severity: string, phase: string}>} $decoded */\n        $decoded = json_decode($raw, true, 512, JSON_THROW_ON_ERROR);\n\n        $entries = [];\n        foreach ($decoded['errors'] as $row) {\n            $entries[$row['code']] = new CatalogEntry(\n                code: ErrorCode::from($row['code']),\n                title: $row['title'],\n                message: $row['message'],\n                fix: $row['fix'],\n                docsUrl: self::DOCS_BASE . strtolower($row['code']),\n                severity: $row['severity'],\n                phase: $row['phase'],\n            );\n        }\n\n        return self::$entries = $entries;\n    }\n}\n",
  "/atoms/core/src/Errors/ErrorCode.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Errors;\n\n/**\n * String-backed enum of every ATOMS-E### code in resources/errors.json.\n *\n * The JSON catalog is the single source of truth; this enum must stay in sync\n * with it (a core test enforces the bidirectional match). Append-only — never\n * renumber an existing case.\n */\nenum ErrorCode: string\n{\n    case UnclassifiableFile = 'ATOMS-E001';\n    case FrameworkSymbolInAtom = 'ATOMS-E010';\n    case FrameworkHelperInAtom = 'ATOMS-E011';\n    case MonolithClassInAtom = 'ATOMS-E012';\n    case UndeclaredPackage = 'ATOMS-E013';\n    case FacadeInAtom = 'ATOMS-E014';\n    case SharedNonCoreSymbol = 'ATOMS-E015';\n    case SharedContainsBehavior = 'ATOMS-E016';\n    case EnvInAtom = 'ATOMS-E017';\n    case NativeSerializationAtBoundary = 'ATOMS-E018';\n    case ExtensionUnavailable = 'ATOMS-E019';\n    case BoundaryTypeOutsideAlgebra = 'ATOMS-E020';\n    case OrmObjectAtBoundary = 'ATOMS-E021';\n    case UnserializableValue = 'ATOMS-E022';\n    case PayloadNotHydratable = 'ATOMS-E023';\n    case BoundaryTypeMismatch = 'ATOMS-E024';\n    case UnknownMethodsMethod = 'ATOMS-E030';\n    case MethodsSignatureMismatch = 'ATOMS-E031';\n    case AtomJobSignatureMismatch = 'ATOMS-E032';\n    case NotAnAtomJob = 'ATOMS-E033';\n    case ManifestHashMismatch = 'ATOMS-E040';\n    case MethodNotInDeployedVersion = 'ATOMS-E041';\n    case BundleRejected = 'ATOMS-E042';\n    case CoreVersionUnsupported = 'ATOMS-E043';\n    case MigrationEdited = 'ATOMS-E050';\n    case MigrationNumberingConflict = 'ATOMS-E051';\n    case MigrationBudgetExceeded = 'ATOMS-E052';\n    case MigrationFailed = 'ATOMS-E053';\n    case AtomTypeNotDeployed = 'ATOMS-E060';\n    case TurnDeadlineExceeded = 'ATOMS-E061';\n    case CapacityRefused = 'ATOMS-E062';\n    case RemoteAtomException = 'ATOMS-E063';\n    case CallbackSignatureInvalid = 'ATOMS-E064';\n    case CallbackReplayDetected = 'ATOMS-E065';\n    case NoMethodsClassForCallback = 'ATOMS-E066';\n    case AtomsJsonInvalid = 'ATOMS-E070';\n    case AtomsComposerJsonInvalid = 'ATOMS-E071';\n    case DeployCredentialsMissing = 'ATOMS-E072';\n    case WranglerNotFound = 'ATOMS-E073';\n    case WranglerFailed = 'ATOMS-E074';\n    case CloudflareAccountMissing = 'ATOMS-E075';\n    case WorkerDirectoryInvalid = 'ATOMS-E076';\n    case SecretNotReadable = 'ATOMS-E077';\n    case BundlePathTooLong = 'ATOMS-E078';\n    case CallbackChannelNotConfigured = 'ATOMS-E080';\n    case CallbackSigningKeyUnusable = 'ATOMS-E081';\n    case CallbackInTransaction = 'ATOMS-E082';\n    case CallbackRequestFailed = 'ATOMS-E083';\n    case JobNotEncodable = 'ATOMS-E084';\n    case InvalidTimerName = 'ATOMS-E085';\n    case TimerLimitExceeded = 'ATOMS-E086';\n}\n",
  "/atoms/core/src/Migrations/Migration.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Migrations;\n\nuse Atoms\\Database;\n\n/**\n * A PHP data migration. Shipped as `NNN_name.php` returning an instance of this\n * interface; the runtime calls {@see up()} inside the activation path, under the\n * Atom's single-writer guarantee.\n */\ninterface Migration\n{\n    public function up(Database $db): void;\n}\n",
  "/atoms/core/src/Migrations/MigrationEntry.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Migrations;\n\nuse Atoms\\Errors\\AtomsError;\nuse Atoms\\Errors\\ErrorCode;\n\n/**\n * One ordered migration within a {@see MigrationSet}: its version, name, the\n * sha256 of its file contents, and the payload (raw SQL, or the path to a PHP\n * migration file).\n *\n * PHP migration files are loaded lazily via {@see migration()} — only the\n * runtime/harness calls that, at apply time. Build-time consumers (validate,\n * manifest generation) read only the metadata and hashes, so customer code is\n * never executed during a build.\n */\nfinal class MigrationEntry\n{\n    private ?Migration $loaded = null;\n\n    public function __construct(\n        public readonly int $version,\n        public readonly string $name,\n        public readonly string $sha256,\n        public readonly ?string $sql,\n        public readonly ?string $phpFile,\n    ) {\n    }\n\n    public function isSql(): bool\n    {\n        return $this->sql !== null;\n    }\n\n    /**\n     * Load the PHP migration object (apply time only — executes the file).\n     *\n     * @throws AtomsError ErrorCode E053 when the file does not return a Migration\n     */\n    public function migration(): ?Migration\n    {\n        if ($this->phpFile === null) {\n            return null;\n        }\n\n        if ($this->loaded === null) {\n            $migration = require $this->phpFile;\n            if (!$migration instanceof Migration) {\n                throw new AtomsError(\n                    ErrorCode::MigrationFailed,\n                    sprintf(\n                        'Migration %s must return an instance of %s.',\n                        basename($this->phpFile),\n                        Migration::class,\n                    ),\n                );\n            }\n            $this->loaded = $migration;\n        }\n\n        return $this->loaded;\n    }\n}\n",
  "/atoms/core/src/Migrations/MigrationSet.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Migrations;\n\nuse Atoms\\Errors\\AtomsError;\nuse Atoms\\Errors\\ErrorCode;\n\n/**\n * An ordered collection of an Atom type's migrations, loaded from a directory of\n * `NNN_name.sql` and `NNN_name.php` files. Validates strictly-increasing, unique\n * integer version prefixes; content-hashes each migration for the manifest.\n *\n * @implements \\IteratorAggregate<int, MigrationEntry>\n */\nfinal class MigrationSet implements \\IteratorAggregate, \\Countable\n{\n    /**\n     * @param list<MigrationEntry> $entries ordered by ascending version\n     */\n    private function __construct(\n        private readonly array $entries,\n    ) {\n    }\n\n    /**\n     * Scan $dir for migration files. Missing or empty directory → empty set.\n     *\n     * @throws AtomsError ErrorCode E051 on a duplicate or non-numeric version prefix\n     */\n    public static function fromDirectory(string $dir): self\n    {\n        if (!is_dir($dir)) {\n            return new self([]);\n        }\n\n        $files = glob(rtrim($dir, '/') . '/*.{sql,php}', GLOB_BRACE);\n        if ($files === false) {\n            $files = [];\n        }\n        sort($files, SORT_STRING);\n\n        /** @var array<int, MigrationEntry> $byVersion */\n        $byVersion = [];\n\n        foreach ($files as $file) {\n            $basename = basename($file);\n\n            if (!preg_match('/^(\\d+)_(.+)\\.(sql|php)$/', $basename, $m)) {\n                throw new AtomsError(\n                    ErrorCode::MigrationNumberingConflict,\n                    \"Migration file {$basename} must be named NNN_name.sql or NNN_name.php.\",\n                );\n            }\n\n            $version = (int) $m[1];\n            $name = $m[2];\n            $extension = $m[3];\n\n            if (isset($byVersion[$version])) {\n                throw new AtomsError(\n                    ErrorCode::MigrationNumberingConflict,\n                    \"Duplicate migration version {$version} at {$basename}.\",\n                );\n            }\n\n            $contents = file_get_contents($file);\n            if ($contents === false) {\n                throw new AtomsError(\n                    ErrorCode::MigrationNumberingConflict,\n                    \"Could not read migration {$basename}.\",\n                );\n            }\n            $sha256 = hash('sha256', $contents);\n\n            if ($extension === 'sql') {\n                $byVersion[$version] = new MigrationEntry($version, $name, $sha256, $contents, null);\n            } else {\n                // Deliberately NOT loaded here: builds/validation must never\n                // execute customer code. The file is require'd lazily by\n                // MigrationEntry::migration() at apply time.\n                $byVersion[$version] = new MigrationEntry($version, $name, $sha256, null, $file);\n            }\n        }\n\n        ksort($byVersion);\n\n        return new self(array_values($byVersion));\n    }\n\n    public function headVersion(): int\n    {\n        if ($this->entries === []) {\n            return 0;\n        }\n\n        return $this->entries[array_key_last($this->entries)]->version;\n    }\n\n    /**\n     * @return list<MigrationEntry>\n     */\n    public function all(): array\n    {\n        return $this->entries;\n    }\n\n    public function count(): int\n    {\n        return \\count($this->entries);\n    }\n\n    public function getIterator(): \\Traversable\n    {\n        return new \\ArrayIterator($this->entries);\n    }\n}\n",
  "/atoms/core/src/Migrations/Migrator.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Migrations;\n\nuse Atoms\\Database;\nuse Atoms\\Errors\\AtomsError;\nuse Atoms\\Errors\\ErrorCode;\n\n/**\n * Applies a {@see MigrationSet} to a {@see Database}, tracking the applied\n * version in SQLite's `user_version` pragma. Each migration runs in its own\n * transaction; a failure leaves `user_version` at the last successful migration.\n */\nfinal class Migrator\n{\n    /**\n     * Apply every pending migration in order, stopping at head.\n     *\n     * @return int the number of migrations applied\n     * @throws AtomsError ErrorCode E053 if a migration fails\n     */\n    public function apply(Database $db, MigrationSet $set): int\n    {\n        $pdo = $db->pdo();\n        $current = (int) $pdo->query('PRAGMA user_version')->fetchColumn();\n        $applied = 0;\n\n        foreach ($set as $entry) {\n            if ($entry->version <= $current) {\n                continue;\n            }\n\n            $pdo->beginTransaction();\n\n            try {\n                if ($entry->isSql()) {\n                    $pdo->exec((string) $entry->sql);\n                } else {\n                    $entry->migration()?->up($db);\n                }\n\n                // user_version is stored in the DB header and is transactional.\n                $pdo->exec('PRAGMA user_version = ' . $entry->version);\n                $pdo->commit();\n            } catch (\\Throwable $e) {\n                if ($pdo->inTransaction()) {\n                    $pdo->rollBack();\n                }\n\n                throw new AtomsError(\n                    ErrorCode::MigrationFailed,\n                    sprintf('Migration %03d_%s failed: %s', $entry->version, $entry->name, $e->getMessage()),\n                    $e,\n                );\n            }\n\n            $current = $entry->version;\n            $applied++;\n        }\n\n        return $applied;\n    }\n}\n",
  "/atoms/core/src/Runtime/AtomContext.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Runtime;\n\nuse Atoms\\AtomJob;\nuse Atoms\\Database;\nuse Atoms\\Timers\\Timers;\n\n/**\n * Everything an Atom needs from its host runtime, behind one interface. The\n * platform runtime and the test harness each provide an implementation; the\n * {@see \\Atoms\\Atom} base class delegates its protected accessors here.\n */\ninterface AtomContext\n{\n    public function db(): Database;\n\n    /**\n     * The `app()` proxy — reverse RPC into the monolith's Methods class.\n     */\n    public function app(): object;\n\n    public function dispatch(AtomJob $job): void;\n\n    public function config(string $key): mixed;\n\n    /**\n     * @param array<string, mixed> $payload\n     */\n    public function broadcast(string $channel, array $payload): void;\n\n    /**\n     * Named one-shot timers scheduled against this Atom.\n     */\n    public function timers(): Timers;\n}\n",
  "/atoms/core/src/Runtime/LifecycleInvoker.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Runtime;\n\nuse Atoms\\Atom;\n\n/**\n * Invokes an Atom's protected lifecycle hooks from outside the class. The\n * runtime and the test harness call these at activation/deactivation; they use\n * Closure::bind rather than reflection so the calls type-check cleanly.\n */\nfinal class LifecycleInvoker\n{\n    private function __construct()\n    {\n    }\n\n    /**\n     * @param Atom<\\Atoms\\AtomMethods> $atom\n     */\n    public static function activate(Atom $atom): void\n    {\n        (function (): void {\n            /** @var Atom<\\Atoms\\AtomMethods> $this */\n            $this->onActivation();\n        })->call($atom);\n    }\n\n    /**\n     * @param Atom<\\Atoms\\AtomMethods> $atom\n     */\n    public static function deactivate(Atom $atom): void\n    {\n        (function (): void {\n            /** @var Atom<\\Atoms\\AtomMethods> $this */\n            $this->onDeactivation();\n        })->call($atom);\n    }\n\n    /**\n     * @param Atom<\\Atoms\\AtomMethods> $atom\n     */\n    public static function timer(Atom $atom, string $name): void\n    {\n        (function (string $name): void {\n            /** @var Atom<\\Atoms\\AtomMethods> $this */\n            $this->onTimer($name);\n        })->call($atom, $name);\n    }\n}\n",
  "/atoms/core/src/Serialization/Payload.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Serialization;\n\n/**\n * Marker interface for boundary DTOs. A Payload's state must be declared as\n * promoted public constructor properties whose types are drawn from the\n * serialization algebra; the {@see Serializer} normalizes it to a JSON object of\n * those properties and hydrates it back by parameter name.\n */\ninterface Payload\n{\n}\n",
  "/atoms/core/src/Serialization/SerializationException.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Serialization;\n\nuse Atoms\\Errors\\AtomsError;\n\n/**\n * Thrown when a value cannot cross the Atoms boundary — either it is outside the\n * serialization algebra (normalize) or the wire value does not match the\n * declared type (denormalize). Always carries the relevant ErrorCode.\n */\nfinal class SerializationException extends AtomsError\n{\n}\n",
  "/atoms/core/src/Serialization/Serializer.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Serialization;\n\nuse Atoms\\Errors\\ErrorCode;\n\n/**\n * Implements the Atoms serialization type algebra (docs/conventions.md).\n *\n * Legal boundary types: null, bool, int, float, string; lists/maps of legal\n * types; {@see Payload} DTOs (promoted constructor properties, nesting allowed);\n * \\DateTimeImmutable ⇄ RFC 3339; \\BackedEnum ⇄ its backed value.\n *\n * Illegal (rejected at runtime with a {@see SerializationException}): closures,\n * resources, \\DateTime (mutable), generic objects, non-hydratable Payloads.\n */\nfinal class Serializer\n{\n    /**\n     * RFC 3339 with microseconds preserved and an explicit numeric offset.\n     */\n    public const DATETIME_FORMAT = 'Y-m-d\\TH:i:s.uP';\n\n    /**\n     * Convert a value into a JSON-safe tree.\n     *\n     * @throws SerializationException if the value is outside the algebra\n     */\n    public function normalize(mixed $value): mixed\n    {\n        if (is_resource($value)) {\n            throw new SerializationException(ErrorCode::UnserializableValue, 'A resource cannot cross the Atoms boundary.');\n        }\n\n        if ($value === null || is_bool($value) || is_int($value) || is_string($value)) {\n            return $value;\n        }\n\n        if (is_float($value)) {\n            if (is_nan($value) || is_infinite($value)) {\n                throw new SerializationException(ErrorCode::UnserializableValue, 'A non-finite float cannot cross the Atoms boundary.');\n            }\n\n            return $value;\n        }\n\n        if (is_array($value)) {\n            $out = [];\n            foreach ($value as $key => $item) {\n                $out[$key] = $this->normalize($item);\n            }\n\n            return $out;\n        }\n\n        if (is_object($value)) {\n            return $this->normalizeObject($value);\n        }\n\n        throw new SerializationException(\n            ErrorCode::UnserializableValue,\n            'A value of type ' . gettype($value) . ' cannot cross the Atoms boundary.',\n        );\n    }\n\n    private function normalizeObject(object $value): mixed\n    {\n        if ($value instanceof \\Closure) {\n            throw new SerializationException(ErrorCode::UnserializableValue, 'A Closure cannot cross the Atoms boundary.');\n        }\n\n        if ($value instanceof \\DateTimeImmutable) {\n            return $value->format(self::DATETIME_FORMAT);\n        }\n\n        if ($value instanceof \\DateTime) {\n            throw new SerializationException(\n                ErrorCode::UnserializableValue,\n                '\\DateTime (mutable) cannot cross the Atoms boundary; use \\DateTimeImmutable.',\n            );\n        }\n\n        if ($value instanceof \\BackedEnum) {\n            return $value->value;\n        }\n\n        if ($value instanceof Payload) {\n            return $this->normalizePayload($value);\n        }\n\n        throw new SerializationException(\n            ErrorCode::UnserializableValue,\n            'An instance of ' . $value::class . ' cannot cross the Atoms boundary.',\n        );\n    }\n\n    /**\n     * @return array<string, mixed>\n     */\n    private function normalizePayload(Payload $value): array\n    {\n        $reflection = new \\ReflectionClass($value);\n        $constructor = $reflection->getConstructor();\n\n        if ($constructor === null) {\n            return [];\n        }\n\n        $out = [];\n        foreach ($constructor->getParameters() as $param) {\n            if (!$param->isPromoted()) {\n                throw new SerializationException(\n                    ErrorCode::PayloadNotHydratable,\n                    'Payload class ' . $value::class . ' must declare all state as promoted constructor properties.',\n                );\n            }\n\n            $property = $reflection->getProperty($param->getName());\n            $out[$param->getName()] = $this->normalize($property->getValue($value));\n        }\n\n        return $out;\n    }\n\n    /**\n     * Hydrate a wire value into a PHP value of the given declared type.\n     *\n     * @param string $type PHP type syntax: 'int','float','bool','string','null',\n     *                     'mixed','array', a nullable '?T', a \\DateTimeImmutable\n     *                     FQCN, a \\BackedEnum FQCN, or a {@see Payload} FQCN.\n     * @throws SerializationException on a type/value mismatch\n     */\n    public function denormalize(mixed $data, string $type): mixed\n    {\n        $nullable = false;\n        if (str_starts_with($type, '?')) {\n            $nullable = true;\n            $type = substr($type, 1);\n        }\n\n        if ($type === 'null') {\n            if ($data !== null) {\n                throw $this->mismatch($data, 'null');\n            }\n\n            return null;\n        }\n\n        if ($type === 'mixed') {\n            return $data;\n        }\n\n        if ($nullable && $data === null) {\n            return null;\n        }\n\n        switch ($type) {\n            case 'int':\n                if (is_int($data)) {\n                    return $data;\n                }\n                throw $this->mismatch($data, 'int');\n            case 'float':\n                if (is_float($data)) {\n                    return $data;\n                }\n                if (is_int($data)) {\n                    return (float) $data; // int→float widening\n                }\n                throw $this->mismatch($data, 'float');\n            case 'bool':\n                if (is_bool($data)) {\n                    return $data;\n                }\n                throw $this->mismatch($data, 'bool');\n            case 'string':\n                if (is_string($data)) {\n                    return $data;\n                }\n                throw $this->mismatch($data, 'string');\n            case 'array':\n                if (is_array($data)) {\n                    return $data;\n                }\n                throw $this->mismatch($data, 'array');\n        }\n\n        $class = ltrim($type, '\\\\');\n\n        if (is_a($class, \\DateTimeImmutable::class, true)) {\n            return $this->denormalizeDateTime($data);\n        }\n\n        if (is_a($class, \\BackedEnum::class, true)) {\n            return $this->denormalizeEnum($data, $class);\n        }\n\n        if (is_a($class, Payload::class, true)) {\n            return $this->denormalizePayload($data, $class);\n        }\n\n        throw $this->mismatch($data, $type);\n    }\n\n    private function denormalizeDateTime(mixed $data): \\DateTimeImmutable\n    {\n        if (!is_string($data)) {\n            throw $this->mismatch($data, \\DateTimeImmutable::class);\n        }\n\n        $dt = \\DateTimeImmutable::createFromFormat(self::DATETIME_FORMAT, $data);\n        if ($dt === false) {\n            throw $this->mismatch($data, \\DateTimeImmutable::class);\n        }\n\n        return $dt;\n    }\n\n    /**\n     * @param class-string<\\BackedEnum> $class\n     */\n    private function denormalizeEnum(mixed $data, string $class): \\BackedEnum\n    {\n        if (!is_int($data) && !is_string($data)) {\n            throw $this->mismatch($data, $class);\n        }\n\n        $case = $class::tryFrom($data);\n        if ($case === null) {\n            throw $this->mismatch($data, $class);\n        }\n\n        return $case;\n    }\n\n    /**\n     * @param class-string $class\n     */\n    private function denormalizePayload(mixed $data, string $class): object\n    {\n        if (!is_array($data)) {\n            throw $this->mismatch($data, $class);\n        }\n\n        $reflection = new \\ReflectionClass($class);\n        $constructor = $reflection->getConstructor();\n\n        if ($constructor === null) {\n            return $reflection->newInstance();\n        }\n\n        $args = [];\n        foreach ($constructor->getParameters() as $param) {\n            $name = $param->getName();\n\n            if (array_key_exists($name, $data)) {\n                $args[] = $this->denormalize($data[$name], $this->parameterType($param));\n                continue;\n            }\n\n            if ($param->isDefaultValueAvailable()) {\n                $args[] = $param->getDefaultValue();\n                continue;\n            }\n\n            if ($param->allowsNull()) {\n                $args[] = null;\n                continue;\n            }\n\n            throw new SerializationException(\n                ErrorCode::BoundaryTypeMismatch,\n                \"Missing required property {$name} hydrating {$class}.\",\n            );\n        }\n\n        return $reflection->newInstanceArgs($args);\n    }\n\n    /**\n     * Denormalize positional arguments against a function/method signature. Used\n     * by client and testing to coerce RPC args. Untyped parameters pass through.\n     *\n     * @param list<mixed> $args\n     * @return list<mixed>\n     */\n    public function denormalizeArguments(array $args, \\ReflectionFunctionAbstract $fn): array\n    {\n        $params = $fn->getParameters();\n        $result = [];\n\n        foreach (array_values($args) as $i => $value) {\n            $param = $params[$i] ?? null;\n\n            if ($param === null || ($param->isVariadic() && !isset($params[$i]))) {\n                $param = $this->variadicParam($params);\n            }\n\n            if ($param === null) {\n                $result[] = $value;\n                continue;\n            }\n\n            $type = $this->parameterType($param);\n            $result[] = $type === 'mixed' ? $value : $this->denormalize($value, $type);\n        }\n\n        return $result;\n    }\n\n    /**\n     * @param list<\\ReflectionParameter> $params\n     */\n    private function variadicParam(array $params): ?\\ReflectionParameter\n    {\n        $last = end($params);\n\n        return ($last !== false && $last->isVariadic()) ? $last : null;\n    }\n\n    /**\n     * Render a parameter's declared type in the syntax denormalize() understands.\n     */\n    private function parameterType(\\ReflectionParameter $param): string\n    {\n        $type = $param->getType();\n\n        if (!$type instanceof \\ReflectionNamedType) {\n            return 'mixed';\n        }\n\n        $name = $type->getName();\n\n        if ($name === 'mixed') {\n            return 'mixed';\n        }\n\n        return ($type->allowsNull() && $name !== 'null') ? '?' . $name : $name;\n    }\n\n    private function mismatch(mixed $data, string $type): SerializationException\n    {\n        return new SerializationException(\n            ErrorCode::BoundaryTypeMismatch,\n            sprintf('Cannot denormalize %s into %s.', $this->describe($data), $type),\n        );\n    }\n\n    private function describe(mixed $data): string\n    {\n        if (is_object($data)) {\n            return $data::class;\n        }\n\n        if (is_array($data)) {\n            return 'array';\n        }\n\n        if (is_string($data)) {\n            return \"'\" . $data . \"'\";\n        }\n\n        if (is_bool($data)) {\n            return $data ? 'true' : 'false';\n        }\n\n        if ($data === null) {\n            return 'null';\n        }\n\n        return (string) $data;\n    }\n}\n",
  "/atoms/core/src/Timers/Timers.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Timers;\n\n/**\n * Named one-shot timers scheduled against this Atom. `schedule()` with a name\n * that already has a pending timer replaces it — there is at most one\n * outstanding timer per name. A past `$at` fires as soon as possible rather\n * than being rejected. Delivery invokes the Atom's `onTimer($name)` hook\n * (via {@see \\Atoms\\Runtime\\LifecycleInvoker}); timers are durable state, not\n * in-memory alarms, and survive hibernation between turns.\n */\ninterface Timers\n{\n    public function schedule(string $name, \\DateTimeImmutable $at): void;\n\n    public function cancel(string $name): void;\n\n    public function scheduledAt(string $name): ?\\DateTimeImmutable;\n}\n",
  "/atoms/core/src/Websocket/Connection.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Websocket;\n\n/**\n * A single WebSocket connection bound to an Atom instance. Implemented by the\n * runtime (and by the test harness); an Atom only ever sees this interface.\n */\ninterface Connection\n{\n    public function id(): string;\n\n    public function send(string $payload): void;\n\n    public function close(int $code = 1000, string $reason = ''): void;\n}\n",
  "/atoms/core/src/Websocket/Message.php": "<?php\n\ndeclare(strict_types=1);\n\nnamespace Atoms\\Websocket;\n\n/**\n * An inbound WebSocket frame delivered to {@see \\Atoms\\Atom::onMessage()}.\n */\ninterface Message\n{\n    public function payload(): string;\n\n    public function isBinary(): bool;\n}\n",
  "/atoms/runtime/AtomsNotSupported.php": "<?php\n\n/**\n * The MVP's one honest failure mode.\n *\n * Everything the Cloudflare MVP does not implement — `app()`, `dispatch()`,\n * `broadcast()`, WebSockets, alarms, and the parts of the PDO surface a\n * hand-written subclass cannot serve — raises this. It is never a silent\n * no-op and never a carrier-database answer (mvp-spec.md §Scope, §PHP-side db()).\n *\n * It extends \\PDOException so that it satisfies the PDO surface's declared\n * failure type; \\PDOException extends \\RuntimeException, so a customer Atom\n * catching \\RuntimeException around a non-PDO call still behaves sanely.\n *\n * No declare(strict_types=1) — see the note in host.php.\n */\n\nnamespace Atoms\\Cf;\n\nclass AtomsNotSupported extends \\PDOException\n{\n    /** The MVP feature that was asked for, e.g. \"PDO::getAttribute\". */\n    public $feature = '';\n\n    /**\n     * @param string $feature the unsupported member or capability\n     * @param string $why one sentence naming the MVP limitation\n     */\n    public function __construct($feature, $why)\n    {\n        $this->feature = (string) $feature;\n\n        parent::__construct(sprintf(\n            '%s is not supported by the Atoms Cloudflare MVP runtime. %s',\n            (string) $feature,\n            (string) $why\n        ));\n\n        // SQLSTATE 0A000 — \"feature not supported\". PDO consumers that inspect\n        // errorInfo() get a real triple rather than an empty one.\n        $this->errorInfo = ['0A000', 0, $this->getMessage()];\n    }\n}\n",
  "/atoms/runtime/AtomsPDO.php": "<?php\n\n/**\n * The \\PDO half of `db()->pdo()`, hardened from the phase-2 spike's\n * `php/pdo_shim.php` (mvp-spec.md §PHP-side db()).\n *\n * The Atoms ABI declares `Database::pdo(): \\PDO`, and the real\n * `Atoms\\Migrations\\Migrator` drives migrations entirely through this surface\n * (`query('PRAGMA user_version')`, `beginTransaction()`, `exec()`, `commit()`,\n * `inTransaction()`, `rollBack()`), so it has to be a genuine \\PDO subclass\n * rather than a duck-typed bridge.\n *\n * The rule for every member: route to the {@see SqlBridge}, or throw\n * {@see AtomsNotSupported}. The spike's remaining leak — `quote()`,\n * `getAttribute()` and `errorInfo()` being answered by the in-memory carrier\n * connection instead of by the Durable Object — is closed here: `quote()` is\n * implemented in PHP with SQLite's own escaping rules, `errorCode()`/\n * `errorInfo()` report the bridge's last statement, and `getAttribute()` serves\n * only the three attributes that have a truthful answer.\n *\n * The carrier `sqlite::memory:` connection exists for one reason: \\PDO's\n * constructor must run for the object to be a usable \\PDO. Nothing is ever\n * executed against it.\n *\n * No declare(strict_types=1) — see the note in host.php.\n */\n\nnamespace Atoms\\Cf;\n\nclass AtomsPDO extends \\PDO\n{\n    /** The DSN of the inert carrier connection. */\n    const CARRIER_DSN = 'sqlite::memory:';\n\n    /** @var SqlBridge */\n    private $bridge;\n\n    /** @var int default fetch mode handed to statements this connection makes */\n    private $fetchMode = \\PDO::FETCH_ASSOC;\n\n    public function __construct(SqlBridge $bridge)\n    {\n        $this->bridge = $bridge;\n\n        parent::__construct(self::CARRIER_DSN);\n    }\n\n    /**\n     * @param string $query\n     * @param array<int, mixed> $options\n     * @return AtomsStatement\n     */\n    #[\\ReturnTypeWillChange]\n    public function prepare($query, $options = [])\n    {\n        if ($options !== []) {\n            throw new AtomsNotSupported(\n                'PDO::prepare() with driver options',\n                'There is no driver-owned statement handle for options to configure.'\n            );\n        }\n\n        return new AtomsStatement($this->bridge, $query, $this->fetchMode);\n    }\n\n    /**\n     * @param string $query\n     * @param int|null $fetchMode\n     * @param mixed ...$fetchModeArgs\n     * @return AtomsStatement\n     */\n    #[\\ReturnTypeWillChange]\n    public function query($query, $fetchMode = null, ...$fetchModeArgs)\n    {\n        if ($fetchModeArgs !== []) {\n            throw new AtomsNotSupported(\n                'PDO::query() with fetch-mode arguments',\n                'Class, callback and column-index variants are not implemented by the MVP shim.'\n            );\n        }\n\n        $mode = $fetchMode === null ? $this->fetchMode : FetchMode::assertSupported($fetchMode, 'PDO::query() fetch mode');\n\n        $statement = new AtomsStatement($this->bridge, $query, $mode);\n        $statement->execute();\n\n        return $statement;\n    }\n\n    /**\n     * @param string $statement\n     * @return int\n     */\n    #[\\ReturnTypeWillChange]\n    public function exec($statement)\n    {\n        $result = $this->bridge->exec((string) $statement, [], SqlBridge::MODE_RUN);\n\n        return $result['rows_written'];\n    }\n\n    /**\n     * PDO's contract is a string, not an int — the Migrator and customer code\n     * both depend on that.\n     *\n     * @param string|null $name\n     * @return string\n     */\n    #[\\ReturnTypeWillChange]\n    public function lastInsertId($name = null)\n    {\n        if ($name !== null) {\n            throw new AtomsNotSupported(\n                'PDO::lastInsertId() with a sequence name',\n                'SQLite has no sequences; call it with no argument.'\n            );\n        }\n\n        return $this->bridge->lastInsertId();\n    }\n\n    /**\n     * @return bool\n     */\n    #[\\ReturnTypeWillChange]\n    public function beginTransaction()\n    {\n        $this->bridge->begin();\n\n        return true;\n    }\n\n    /**\n     * @return bool\n     */\n    #[\\ReturnTypeWillChange]\n    public function commit()\n    {\n        $this->bridge->commit();\n\n        return true;\n    }\n\n    /**\n     * @return bool\n     */\n    #[\\ReturnTypeWillChange]\n    public function rollBack()\n    {\n        $this->bridge->rollback();\n\n        return true;\n    }\n\n    /**\n     * @return bool\n     */\n    #[\\ReturnTypeWillChange]\n    public function inTransaction()\n    {\n        return $this->bridge->inTransaction();\n    }\n\n    /**\n     * SQLite escaping, implemented here rather than delegated to the carrier\n     * connection, so that nothing about this object depends on the carrier.\n     *\n     * @param string $string\n     * @param int $type\n     * @return string\n     */\n    #[\\ReturnTypeWillChange]\n    public function quote($string, $type = \\PDO::PARAM_STR)\n    {\n        switch ($type) {\n            case \\PDO::PARAM_NULL:\n                return 'NULL';\n\n            case \\PDO::PARAM_BOOL:\n                return $string ? '1' : '0';\n\n            case \\PDO::PARAM_INT:\n                return (string) (int) $string;\n\n            case \\PDO::PARAM_STR:\n                return \"'\" . str_replace(\"'\", \"''\", (string) $string) . \"'\";\n        }\n\n        throw new AtomsNotSupported(\n            sprintf('PDO::quote() with parameter type %d', $type),\n            'Only PARAM_NULL, PARAM_BOOL, PARAM_INT and PARAM_STR are implemented; bind values instead of quoting them.'\n        );\n    }\n\n    /**\n     * @return string|null the SQLSTATE of the bridge's last statement\n     */\n    #[\\ReturnTypeWillChange]\n    public function errorCode()\n    {\n        return $this->bridge->errorCode();\n    }\n\n    /**\n     * @return array{0: string, 1: int|null, 2: string|null}\n     */\n    #[\\ReturnTypeWillChange]\n    public function errorInfo()\n    {\n        return $this->bridge->errorInfo();\n    }\n\n    /**\n     * Only the attributes with a truthful answer are served; the rest throw\n     * rather than reporting the carrier connection's values.\n     *\n     * @param int $attribute\n     * @return mixed\n     */\n    #[\\ReturnTypeWillChange]\n    public function getAttribute($attribute)\n    {\n        switch ($attribute) {\n            case \\PDO::ATTR_DRIVER_NAME:\n                return 'sqlite';\n\n            case \\PDO::ATTR_ERRMODE:\n                return \\PDO::ERRMODE_EXCEPTION;\n\n            case \\PDO::ATTR_DEFAULT_FETCH_MODE:\n                return $this->fetchMode;\n        }\n\n        throw new AtomsNotSupported(\n            sprintf('PDO::getAttribute(%d)', $attribute),\n            'Only ATTR_DRIVER_NAME, ATTR_ERRMODE and ATTR_DEFAULT_FETCH_MODE describe the Atoms bridge; '\n            . 'anything else would be the inert carrier connection answering.'\n        );\n    }\n\n    /**\n     * @param int $attribute\n     * @param mixed $value\n     * @return bool\n     */\n    #[\\ReturnTypeWillChange]\n    public function setAttribute($attribute, $value)\n    {\n        if ($attribute === \\PDO::ATTR_ERRMODE) {\n            if ($value === \\PDO::ERRMODE_EXCEPTION) {\n                return true;\n            }\n\n            throw new AtomsNotSupported(\n                'PDO::ATTR_ERRMODE other than ERRMODE_EXCEPTION',\n                'The bridge reports every failure by throwing; silent and warning modes cannot be honoured.'\n            );\n        }\n\n        if ($attribute === \\PDO::ATTR_DEFAULT_FETCH_MODE) {\n            $this->fetchMode = FetchMode::assertSupported($value, 'PDO::ATTR_DEFAULT_FETCH_MODE');\n\n            return true;\n        }\n\n        throw new AtomsNotSupported(\n            sprintf('PDO::setAttribute(%d, ...)', $attribute),\n            'Only ATTR_ERRMODE and ATTR_DEFAULT_FETCH_MODE are meaningful across the Atoms bridge.'\n        );\n    }\n\n    // ---------------------------------------------------------------------\n    // Driver extensions. These are provided by pdo_sqlite on the carrier\n    // connection and would otherwise appear to work while affecting nothing\n    // that the Durable Object executes.\n    // ---------------------------------------------------------------------\n\n    /**\n     * @param string $function\n     * @param callable $callback\n     * @param int $numArgs\n     * @param int $flags\n     * @return bool\n     */\n    #[\\ReturnTypeWillChange]\n    public function sqliteCreateFunction($function, $callback, $numArgs = -1, $flags = 0)\n    {\n        throw new AtomsNotSupported(\n            'PDO::sqliteCreateFunction()',\n            'Statements execute in the Durable Object, not in the guest, so a PHP callback cannot be registered with them.'\n        );\n    }\n\n    /**\n     * @param string $function\n     * @param callable $stepCallback\n     * @param callable $finalizeCallback\n     * @param int $numArgs\n     * @return bool\n     */\n    #[\\ReturnTypeWillChange]\n    public function sqliteCreateAggregate($function, $stepCallback, $finalizeCallback, $numArgs = -1)\n    {\n        throw new AtomsNotSupported(\n            'PDO::sqliteCreateAggregate()',\n            'Statements execute in the Durable Object, not in the guest, so a PHP callback cannot be registered with them.'\n        );\n    }\n\n    /**\n     * @param string $name\n     * @param callable $callback\n     * @return bool\n     */\n    #[\\ReturnTypeWillChange]\n    public function sqliteCreateCollation($name, $callback)\n    {\n        throw new AtomsNotSupported(\n            'PDO::sqliteCreateCollation()',\n            'Statements execute in the Durable Object, not in the guest, so a PHP callback cannot be registered with them.'\n        );\n    }\n}\n",
  "/atoms/runtime/AtomsStatement.php": "<?php\n\n/**\n * The \\PDOStatement half of the documented-leaky `db()->pdo()` surface,\n * hardened from the phase-2 spike's `php/pdo_shim.php`.\n *\n * The Atoms customer ABI declares `Database::pdo(): \\PDO`, so the object handed\n * to customer code must genuinely be a \\PDO / \\PDOStatement — hence the\n * subclass. No driver backs this statement: every member either routes to the\n * {@see SqlBridge} (and therefore to `ctx.storage.sql` in the Durable Object)\n * or throws {@see AtomsNotSupported}. Nothing is ever answered by the carrier\n * connection, and nothing silently no-ops.\n *\n * Known leak, documented rather than papered over: `$stmt->queryString` is set\n * on a best-effort basis only — PDO treats it as a read-only driver property,\n * so on builds that reject the write it stays uninitialized.\n *\n * No declare(strict_types=1) — see the note in host.php.\n */\n\nnamespace Atoms\\Cf;\n\nclass AtomsStatement extends \\PDOStatement\n{\n    /** @var SqlBridge */\n    private $bridge;\n\n    /** @var string the SQL as prepared, with `:name` placeholders intact */\n    private $sql;\n\n    /** @var array<int|string, mixed> values bound with bindValue() */\n    private $bound = [];\n\n    /** @var list<array<string, mixed>> rows from the most recent execute() */\n    private $rows = [];\n\n    /** @var int cursor into $rows */\n    private $cursor = 0;\n\n    /** @var int rows_written reported by the most recent execute() */\n    private $rowsWritten = 0;\n\n    /** @var int one of the PDO::FETCH_* modes this shim supports */\n    private $fetchMode;\n\n    /**\n     * @param SqlBridge $bridge\n     * @param string $sql\n     * @param int $fetchMode default fetch mode inherited from the AtomsPDO\n     */\n    public function __construct(SqlBridge $bridge, $sql, $fetchMode = \\PDO::FETCH_ASSOC)\n    {\n        $this->bridge = $bridge;\n        $this->sql = (string) $sql;\n        $this->fetchMode = FetchMode::assertSupported($fetchMode, 'PDOStatement fetch mode');\n\n        try {\n            // Best effort: PDO exposes queryString as a driver-owned read-only\n            // property, so this write is allowed to fail.\n            $this->queryString = $this->sql;\n        } catch (\\Throwable $ignored) {\n        }\n    }\n\n    /**\n     * @param array<int|string, mixed>|null $params\n     * @return bool\n     */\n    public function execute(?array $params = null): bool\n    {\n        // PDO semantics: an array passed to execute() replaces bound values.\n        $bindings = $params === null ? $this->bound : $params;\n\n        list($sql, $positional) = NamedParams::rewrite($this->sql, $bindings);\n\n        $result = $this->bridge->exec($sql, $positional, SqlBridge::MODE_ROWS);\n\n        $this->rows = $result['rows'];\n        $this->rowsWritten = $result['rows_written'];\n        $this->cursor = 0;\n\n        return true;\n    }\n\n    /**\n     * @param int|string $param 1-based position or `:name` / `name`\n     * @param mixed $value\n     * @param int $type\n     * @return bool\n     */\n    public function bindValue($param, $value, $type = \\PDO::PARAM_STR): bool\n    {\n        if ($type === \\PDO::PARAM_LOB) {\n            throw new AtomsNotSupported(\n                'PDO::PARAM_LOB',\n                'Binary values do not cross the MVP JSON bridge; store them base64-encoded as text.'\n            );\n        }\n\n        $this->bound[$param] = $value;\n\n        return true;\n    }\n\n    /**\n     * @param int $mode\n     * @param int $cursorOrientation\n     * @param int $cursorOffset\n     * @return mixed the row, or false when the cursor is exhausted\n     */\n    #[\\ReturnTypeWillChange]\n    public function fetch($mode = \\PDO::FETCH_DEFAULT, $cursorOrientation = \\PDO::FETCH_ORI_NEXT, $cursorOffset = 0)\n    {\n        if ($cursorOrientation !== \\PDO::FETCH_ORI_NEXT) {\n            throw new AtomsNotSupported(\n                'PDOStatement::fetch() with a scrollable cursor',\n                'The bridge buffers the whole result set; only FETCH_ORI_NEXT is available.'\n            );\n        }\n\n        $mode = $this->resolveMode($mode);\n\n        if (!array_key_exists($this->cursor, $this->rows)) {\n            return false;\n        }\n\n        $row = $this->rows[$this->cursor];\n        $this->cursor++;\n\n        return FetchMode::shape($row, $mode);\n    }\n\n    /**\n     * @param int $mode\n     * @param mixed ...$args\n     * @return array<int|string, mixed>\n     */\n    #[\\ReturnTypeWillChange]\n    public function fetchAll($mode = \\PDO::FETCH_DEFAULT, ...$args)\n    {\n        if ($args !== []) {\n            throw new AtomsNotSupported(\n                'PDOStatement::fetchAll() with fetch-mode arguments',\n                'Class, callback and column-index variants are not implemented by the MVP shim.'\n            );\n        }\n\n        $mode = $this->resolveMode($mode);\n        $remaining = array_slice($this->rows, $this->cursor);\n        $this->cursor = count($this->rows);\n\n        if ($mode === \\PDO::FETCH_COLUMN) {\n            $out = [];\n            foreach ($remaining as $row) {\n                $values = array_values($row);\n                $out[] = array_key_exists(0, $values) ? $values[0] : null;\n            }\n\n            return $out;\n        }\n\n        if ($mode === \\PDO::FETCH_KEY_PAIR) {\n            $out = [];\n            foreach ($remaining as $row) {\n                $values = array_values($row);\n                if (count($values) < 2) {\n                    throw new \\PDOException(\n                        'SQLSTATE[HY000] PDO::FETCH_KEY_PAIR requires exactly two columns.'\n                    );\n                }\n                $out[$values[0]] = $values[1];\n            }\n\n            return $out;\n        }\n\n        $out = [];\n        foreach ($remaining as $row) {\n            $out[] = FetchMode::shape($row, $mode);\n        }\n\n        return $out;\n    }\n\n    /**\n     * @param int $column 0-based column index\n     * @return mixed the value, or false when the cursor is exhausted\n     */\n    #[\\ReturnTypeWillChange]\n    public function fetchColumn($column = 0)\n    {\n        if (!array_key_exists($this->cursor, $this->rows)) {\n            return false;\n        }\n\n        $values = array_values($this->rows[$this->cursor]);\n        $this->cursor++;\n\n        return array_key_exists($column, $values) ? $values[$column] : null;\n    }\n\n    /**\n     * PDO's contract: affected rows for INSERT/UPDATE/DELETE, and undefined\n     * (SQLite reports 0) for SELECT. This reports what the host counted.\n     */\n    public function rowCount(): int\n    {\n        return $this->rowsWritten;\n    }\n\n    public function columnCount(): int\n    {\n        if (!array_key_exists(0, $this->rows)) {\n            return 0;\n        }\n\n        return count($this->rows[0]);\n    }\n\n    public function closeCursor(): bool\n    {\n        $this->rows = [];\n        $this->cursor = 0;\n\n        return true;\n    }\n\n    /**\n     * @param int $mode\n     * @param mixed ...$args\n     * @return bool\n     */\n    #[\\ReturnTypeWillChange]\n    public function setFetchMode($mode, ...$args)\n    {\n        if ($args !== []) {\n            throw new AtomsNotSupported(\n                'PDOStatement::setFetchMode() with mode arguments',\n                'Class, object and column-bound fetch modes are not implemented by the MVP shim.'\n            );\n        }\n\n        $this->fetchMode = FetchMode::assertSupported($mode, 'PDOStatement::setFetchMode()');\n\n        return true;\n    }\n\n    /**\n     * Buffered result set, so foreach over the statement is exact.\n     */\n    public function getIterator(): \\Iterator\n    {\n        return new \\ArrayIterator($this->fetchAll($this->fetchMode));\n    }\n\n    /**\n     * @return string|null\n     */\n    #[\\ReturnTypeWillChange]\n    public function errorCode()\n    {\n        return $this->bridge->errorCode();\n    }\n\n    /**\n     * @return array{0: string, 1: int|null, 2: string|null}\n     */\n    #[\\ReturnTypeWillChange]\n    public function errorInfo()\n    {\n        return $this->bridge->errorInfo();\n    }\n\n    // ---------------------------------------------------------------------\n    // Deliberately unsupported. Each throws; none of them silently no-ops or\n    // falls through to a carrier connection.\n    // ---------------------------------------------------------------------\n\n    /**\n     * @param int|string $param\n     * @param mixed $var\n     * @param int $type\n     * @param int $maxLength\n     * @param mixed $driverOptions\n     * @return bool\n     */\n    #[\\ReturnTypeWillChange]\n    public function bindParam($param, &$var, $type = \\PDO::PARAM_STR, $maxLength = 0, $driverOptions = null)\n    {\n        throw new AtomsNotSupported(\n            'PDOStatement::bindParam()',\n            'By-reference binding needs a driver-owned statement handle; use bindValue() or pass values to execute().'\n        );\n    }\n\n    /**\n     * @param int|string $column\n     * @param mixed $var\n     * @param int $type\n     * @param int $maxLength\n     * @param mixed $driverOptions\n     * @return bool\n     */\n    #[\\ReturnTypeWillChange]\n    public function bindColumn($column, &$var, $type = \\PDO::PARAM_STR, $maxLength = 0, $driverOptions = null)\n    {\n        throw new AtomsNotSupported(\n            'PDOStatement::bindColumn()',\n            'PDO::FETCH_BOUND is not implemented by the MVP shim.'\n        );\n    }\n\n    /**\n     * @param string|null $class\n     * @param array<int, mixed>|null $constructorArgs\n     * @return object|false\n     */\n    #[\\ReturnTypeWillChange]\n    public function fetchObject($class = 'stdClass', $constructorArgs = null)\n    {\n        throw new AtomsNotSupported(\n            'PDOStatement::fetchObject()',\n            'Hydrating arbitrary classes from a row is not implemented; fetch with PDO::FETCH_OBJ or PDO::FETCH_ASSOC.'\n        );\n    }\n\n    /**\n     * @param int $name\n     * @return mixed\n     */\n    #[\\ReturnTypeWillChange]\n    public function getAttribute($name)\n    {\n        throw new AtomsNotSupported(\n            'PDOStatement::getAttribute()',\n            'There is no driver-owned statement handle to read attributes from.'\n        );\n    }\n\n    /**\n     * @param int $attribute\n     * @param mixed $value\n     * @return bool\n     */\n    #[\\ReturnTypeWillChange]\n    public function setAttribute($attribute, $value)\n    {\n        throw new AtomsNotSupported(\n            'PDOStatement::setAttribute()',\n            'There is no driver-owned statement handle to configure.'\n        );\n    }\n\n    /**\n     * @param int $column\n     * @return array<string, mixed>|false\n     */\n    #[\\ReturnTypeWillChange]\n    public function getColumnMeta($column)\n    {\n        throw new AtomsNotSupported(\n            'PDOStatement::getColumnMeta()',\n            'The sql.exec reply carries values only, not column metadata.'\n        );\n    }\n\n    /**\n     * @return bool\n     */\n    #[\\ReturnTypeWillChange]\n    public function nextRowset()\n    {\n        throw new AtomsNotSupported(\n            'PDOStatement::nextRowset()',\n            'The bridge returns a single result set per statement.'\n        );\n    }\n\n    /**\n     * @return void\n     */\n    #[\\ReturnTypeWillChange]\n    public function debugDumpParams()\n    {\n        throw new AtomsNotSupported(\n            'PDOStatement::debugDumpParams()',\n            'It writes to stdout, which is the Durable Object response body in this runtime.'\n        );\n    }\n\n    /**\n     * Resolve PDO::FETCH_DEFAULT against this statement's configured mode.\n     *\n     * @param int $mode\n     * @return int\n     */\n    private function resolveMode($mode)\n    {\n        if ($mode === \\PDO::FETCH_DEFAULT) {\n            return $this->fetchMode;\n        }\n\n        return FetchMode::assertSupported($mode, 'PDOStatement fetch mode');\n    }\n}\n",
  "/atoms/runtime/BootstrapError.php": "<?php\n\n/**\n * A failure in the activation path — before the guest ever parks at\n * `turn.await`, so before there is any turn-result envelope to carry it.\n *\n * These carry one of the spec's error codes (`atom_not_found` or `internal`,\n * mvp-spec.md §Turn-result envelope) so the host can classify the failure from\n * the `log` line that {@see bootstrap.php} emits before rethrowing. The rethrow\n * is deliberate: an activation that did not complete leaves no Atom to serve\n * turns, and the spec's answer to that is a poisoned residency the host\n * discards and re-activates from durable state.\n *\n * No declare(strict_types=1) — see the note in host.php.\n */\n\nnamespace Atoms\\Cf;\n\nclass BootstrapError extends \\RuntimeException\n{\n    /** @var string one of the mvp-spec error codes */\n    private $atomsCode;\n\n    /**\n     * @param string $atomsCode 'atom_not_found' | 'internal'\n     * @param string $message\n     * @param \\Throwable|null $previous\n     */\n    public function __construct($atomsCode, $message, ?\\Throwable $previous = null)\n    {\n        $this->atomsCode = (string) $atomsCode;\n\n        parent::__construct($message, 0, $previous);\n    }\n\n    /**\n     * @return string\n     */\n    public function atomsCode()\n    {\n        return $this->atomsCode;\n    }\n}\n",
  "/atoms/runtime/BridgeDatabase.php": "<?php\n\n/**\n * `Atoms\\Database` over the Durable Object's own SQLite (mvp-spec.md\n * §PHP-side db()).\n *\n * This is the platform-side implementation the customer's Atom actually gets\n * from `$this->db()`. It is deliberately shaped like the reference\n * `Atoms\\Sqlite\\SqliteDatabase`:\n *\n *  - `query()` / `execute()` prepare-by-rewriting named parameters to\n *    positional **in PHP** (the wire only carries positional bindings), tag\n *    int64 values, and call the `sql.exec` door.\n *  - `transaction()` reproduces SqliteDatabase's nesting guard verbatim in\n *    behaviour — an already-open transaction is reused rather than nested,\n *    because SQLite has no nested BEGIN and the Durable Object has no\n *    savepoints — and then drives tx.begin → $fn → tx.commit, rolling back and\n *    rethrowing on any Throwable.\n *  - `pdo()` returns the hardened {@see AtomsPDO}, sharing this object's\n *    {@see SqlBridge} so both views agree about whether a transaction is open.\n *\n * No declare(strict_types=1) — see the note in host.php.\n */\n\nnamespace Atoms\\Cf;\n\nuse Atoms\\Database;\n\nfinal class BridgeDatabase implements Database\n{\n    /** @var SqlBridge */\n    private $bridge;\n\n    /** @var AtomsPDO|null lazily built; one per Atom, like one connection */\n    private $pdo = null;\n\n    public function __construct(SqlBridge $bridge)\n    {\n        $this->bridge = $bridge;\n    }\n\n    /**\n     * The documented-leaky \\PDO view of the same connection.\n     */\n    public function pdo(): \\PDO\n    {\n        if ($this->pdo === null) {\n            $this->pdo = new AtomsPDO($this->bridge);\n        }\n\n        return $this->pdo;\n    }\n\n    /**\n     * @param array<int|string, mixed> $bindings\n     * @return list<array<string, mixed>>\n     */\n    public function query(string $sql, array $bindings = []): array\n    {\n        list($rewritten, $positional) = $this->toPositional($sql, $bindings);\n\n        $result = $this->bridge->exec($rewritten, $positional, SqlBridge::MODE_ROWS);\n\n        return $result['rows'];\n    }\n\n    /**\n     * @param array<int|string, mixed> $bindings\n     */\n    public function execute(string $sql, array $bindings = []): int\n    {\n        list($rewritten, $positional) = $this->toPositional($sql, $bindings);\n\n        $result = $this->bridge->exec($rewritten, $positional, SqlBridge::MODE_RUN);\n\n        return $result['rows_written'];\n    }\n\n    /**\n     * @param callable(Database): mixed $fn\n     */\n    public function transaction(callable $fn): mixed\n    {\n        // Nesting guard, identical in intent to SqliteDatabase::transaction():\n        // reuse an already-open outer transaction; the outer call owns\n        // commit/rollback. The host enforces the same rule as defence in depth.\n        if ($this->bridge->inTransaction()) {\n            return $fn($this);\n        }\n\n        $this->bridge->begin();\n\n        try {\n            $result = $fn($this);\n            $this->bridge->commit();\n\n            return $result;\n        } catch (\\Throwable $e) {\n            try {\n                $this->bridge->rollback();\n            } catch (\\PDOException $ignored) {\n                // Transaction already closed (e.g. the commit itself failed).\n                // The original throwable is the one that matters.\n            }\n\n            throw $e;\n        }\n    }\n\n    /**\n     * @param string $sql\n     * @param array<int|string, mixed> $bindings\n     * @return array{0: string, 1: list<mixed>}\n     */\n    private function toPositional($sql, array $bindings)\n    {\n        return NamedParams::rewrite($sql, $bindings);\n    }\n}\n",
  "/atoms/runtime/CfAtomContext.php": "<?php\n\n/**\n * `Atoms\\Runtime\\AtomContext` for the Cloudflare MVP — everything the customer's\n * Atom is allowed to reach, and the exact boundary of what the MVP implements.\n *\n * `db()` and `config()` are real. `app()`, `dispatch()` and `broadcast()` are\n * out of scope for the MVP and throw {@see AtomsNotSupported} naming the\n * limitation (mvp-spec.md §Scope: \"These are explicit stubs, never silent\n * no-ops\"). A customer Atom that calls them fails its turn with a clear\n * `atom_exception` envelope rather than appearing to have sent something.\n *\n * No declare(strict_types=1) — see the note in host.php.\n */\n\nnamespace Atoms\\Cf;\n\nuse Atoms\\AtomJob;\nuse Atoms\\Database;\nuse Atoms\\Runtime\\AtomContext;\nuse Atoms\\Timers\\Timers;\n\nfinal class CfAtomContext implements AtomContext\n{\n    /** @var BridgeDatabase */\n    private $db;\n\n    public function __construct(BridgeDatabase $db)\n    {\n        $this->db = $db;\n    }\n\n    /**\n     * One database per Atom, shared by every turn of this residency.\n     */\n    public function db(): Database\n    {\n        return $this->db;\n    }\n\n    /**\n     * Resolved by the host from an allowlisted view of the Worker's `env`\n     * (mvp-spec.md §Sync ops). Unknown keys are null, not an error — same as the\n     * platform runtime.\n     */\n    public function config(string $key): mixed\n    {\n        $reply = host_sync(['op' => 'config.get', 'key' => $key]);\n\n        if (!array_key_exists('value', $reply)) {\n            return null;\n        }\n\n        return int64_decode($reply['value']);\n    }\n\n    public function app(): object\n    {\n        throw new AtomsNotSupported(\n            'Atom::app()',\n            'Reverse RPC into the monolith needs a control plane and a callback channel; '\n            . 'the Cloudflare MVP Worker is single-tenant and has neither.'\n        );\n    }\n\n    public function dispatch(AtomJob $job): void\n    {\n        throw new AtomsNotSupported(\n            'Atom::dispatch()',\n            sprintf(\n                'Job dispatch (%s) needs the monolith queue seam, which the Cloudflare MVP does not implement.',\n                get_class($job)\n            )\n        );\n    }\n\n    /**\n     * @param array<string, mixed> $payload\n     */\n    public function broadcast(string $channel, array $payload): void\n    {\n        throw new AtomsNotSupported(\n            'Atom::broadcast()',\n            sprintf(\n                'WebSockets are out of scope for the Cloudflare MVP, so there is nothing subscribed to \"%s\".',\n                $channel\n            )\n        );\n    }\n\n    // Replaced by the timers wave.\n    public function timers(): Timers\n    {\n        throw new AtomsNotSupported(\n            'Atom::timers()',\n            'M2 wave 0 adds Atoms\\Timers\\Timers to the AtomContext ABI so atoms-core '\n            . 'vendors cleanly, but a later M2 wave is what actually schedules timers '\n            . 'on this Worker runtime.'\n        );\n    }\n}\n",
  "/atoms/runtime/FetchMode.php": "<?php\n\n/**\n * The subset of PDO fetch modes the MVP shim serves, and the row reshaping for\n * each. Anything outside the subset raises {@see AtomsNotSupported} rather than\n * quietly degrading to associative rows.\n *\n * The bridge always receives rows as `{column: value}` maps from\n * `ctx.storage.sql`, so every supported mode is a pure reshaping of that.\n *\n * No declare(strict_types=1) — see the note in host.php.\n */\n\nnamespace Atoms\\Cf;\n\nfinal class FetchMode\n{\n    private function __construct()\n    {\n    }\n\n    /**\n     * @return list<int> the modes this shim implements\n     */\n    public static function supported()\n    {\n        return [\n            \\PDO::FETCH_ASSOC,\n            \\PDO::FETCH_NUM,\n            \\PDO::FETCH_BOTH,\n            \\PDO::FETCH_OBJ,\n            \\PDO::FETCH_COLUMN,\n            \\PDO::FETCH_KEY_PAIR,\n        ];\n    }\n\n    /**\n     * @param int $mode\n     * @param string $context what asked, for the exception message\n     * @return int the normalized mode (FETCH_DEFAULT becomes FETCH_ASSOC)\n     * @throws AtomsNotSupported\n     */\n    public static function assertSupported($mode, $context)\n    {\n        $mode = (int) $mode;\n\n        if ($mode === \\PDO::FETCH_DEFAULT) {\n            return \\PDO::FETCH_ASSOC;\n        }\n\n        if (in_array($mode, self::supported(), true)) {\n            return $mode;\n        }\n\n        throw new AtomsNotSupported(\n            sprintf('%s %d', $context, $mode),\n            'Supported modes are FETCH_ASSOC, FETCH_NUM, FETCH_BOTH, FETCH_OBJ, FETCH_COLUMN and FETCH_KEY_PAIR.'\n        );\n    }\n\n    /**\n     * Reshape one `{column: value}` row into the requested mode.\n     *\n     * @param array<string, mixed> $row\n     * @param int $mode already normalized by assertSupported()\n     * @return mixed\n     * @throws AtomsNotSupported for modes that are meaningless for a single row\n     */\n    public static function shape(array $row, $mode)\n    {\n        switch ($mode) {\n            case \\PDO::FETCH_ASSOC:\n                return $row;\n\n            case \\PDO::FETCH_NUM:\n                return array_values($row);\n\n            case \\PDO::FETCH_BOTH:\n                $out = [];\n                $index = 0;\n                foreach ($row as $column => $value) {\n                    $out[$column] = $value;\n                    $out[$index] = $value;\n                    $index++;\n                }\n\n                return $out;\n\n            case \\PDO::FETCH_OBJ:\n                return (object) $row;\n\n            case \\PDO::FETCH_COLUMN:\n                $values = array_values($row);\n\n                return array_key_exists(0, $values) ? $values[0] : null;\n        }\n\n        throw new AtomsNotSupported(\n            sprintf('fetch mode %d for a single row', $mode),\n            'PDO::FETCH_KEY_PAIR is only meaningful for fetchAll().'\n        );\n    }\n}\n",
  "/atoms/runtime/MigrationsGlobShim.php": "<?php\n\n/**\n * Host compatibility shim for `Atoms\\Migrations\\MigrationSet::fromDirectory()`.\n *\n * MEASURED (php-wasm 8.3.32 Asyncify build, wrangler dev, 2026-08-04):\n * this build's libc `glob()` has no brace support, so\n *\n *     defined('GLOB_BRACE')                        === false\n *     glob('/app/migrations/*.{sql,php}', 1024)    === false\n *     glob('/app/migrations/*.{sql,php}', 0)       === []\n *\n * while plain patterns (`/app/migrations/*.sql`) work correctly. The verbatim\n * `MigrationSet::fromDirectory()` uses exactly the brace form, so without this\n * file every Atom activates with *zero* discovered migrations — the one failure\n * mode this runtime must never have. (The bootstrap's count guard turns it into\n * a loud activation failure rather than a silent one, which is how it was\n * found.)\n *\n * atoms-core is verbatim and is never patched, so the fix lives here instead:\n * PHP resolves an unqualified function call or constant inside a namespace\n * against that namespace FIRST and only then against the global scope. Both\n * declarations below therefore shadow the missing libc feature for\n * `MigrationSet` alone — nothing else in the guest sees a different `glob()`.\n *\n * The shim is exact, not best-effort: it expands the brace alternatives itself\n * and delegates each expansion to the real `\\glob()`, preserving the platform's\n * ordering contract by leaving `MigrationSet`'s own `sort()` to do the ordering.\n *\n * No declare(strict_types=1) — see the note in host.php.\n */\n\nnamespace Atoms\\Migrations;\n\n/**\n * The value glibc uses. Only ever passed back to this file's own `glob()`,\n * which strips it before calling the real one.\n */\nconst GLOB_BRACE = 1024;\n\n/**\n * Brace-aware `glob()` for the guest.\n *\n * @param string $pattern\n * @param int $flags\n * @return list<string>|false the real `\\glob()`'s answer, false on failure\n */\nfunction glob($pattern, $flags = 0)\n{\n    $expansions = expand_braces((string) $pattern);\n\n    // Nothing to expand: this is the plain form the build already handles.\n    if (count($expansions) === 1 && $expansions[0] === (string) $pattern) {\n        return \\glob($pattern, $flags & ~GLOB_BRACE);\n    }\n\n    $matches = [];\n\n    foreach ($expansions as $expanded) {\n        $found = \\glob($expanded, $flags & ~GLOB_BRACE);\n\n        if ($found === false) {\n            // Preserve \\glob()'s own failure signal rather than reporting an\n            // empty directory, which would silently skip migrations.\n            return false;\n        }\n\n        foreach ($found as $path) {\n            $matches[$path] = true;\n        }\n    }\n\n    return array_keys($matches);\n}\n\n/**\n * Expand every `{a,b,c}` alternative in a glob pattern, outermost first and\n * recursively, the way glibc's GLOB_BRACE does.\n *\n * @param string $pattern\n * @return list<string>\n */\nfunction expand_braces($pattern)\n{\n    $open = -1;\n    $depth = 0;\n\n    for ($i = 0, $len = strlen($pattern); $i < $len; $i++) {\n        $char = $pattern[$i];\n\n        if ($char === '\\\\') {\n            $i++; // escaped character, never a brace\n            continue;\n        }\n\n        if ($char === '{') {\n            if ($depth === 0) {\n                $open = $i;\n            }\n            $depth++;\n            continue;\n        }\n\n        if ($char === '}' && $depth > 0) {\n            $depth--;\n\n            if ($depth === 0) {\n                $prefix = substr($pattern, 0, $open);\n                $suffix = substr($pattern, $i + 1);\n                $body = substr($pattern, $open + 1, $i - $open - 1);\n\n                $out = [];\n                foreach (split_alternatives($body) as $alternative) {\n                    foreach (expand_braces($prefix . $alternative . $suffix) as $expanded) {\n                        $out[] = $expanded;\n                    }\n                }\n\n                return $out;\n            }\n        }\n    }\n\n    return [$pattern];\n}\n\n/**\n * Split a brace body on its top-level commas, ignoring commas nested in inner\n * braces or escaped.\n *\n * @param string $body\n * @return list<string>\n */\nfunction split_alternatives($body)\n{\n    $parts = [];\n    $current = '';\n    $depth = 0;\n\n    for ($i = 0, $len = strlen($body); $i < $len; $i++) {\n        $char = $body[$i];\n\n        if ($char === '\\\\' && $i + 1 < $len) {\n            $current .= $char . $body[$i + 1];\n            $i++;\n            continue;\n        }\n\n        if ($char === '{') {\n            $depth++;\n        } elseif ($char === '}') {\n            $depth--;\n        } elseif ($char === ',' && $depth === 0) {\n            $parts[] = $current;\n            $current = '';\n            continue;\n        }\n\n        $current .= $char;\n    }\n\n    $parts[] = $current;\n\n    return $parts;\n}\n",
  "/atoms/runtime/NamedParams.php": "<?php\n\n/**\n * Named→positional parameter rewriting, done in PHP.\n *\n * The `sql.exec` wire op carries positional bindings only (mvp-spec.md\n * §Sync ops), so `:name` placeholders are resolved on this side of the door —\n * shared by {@see BridgeDatabase} (Atoms\\Database) and {@see AtomsStatement}\n * (the \\PDO surface) so both accept exactly the same SQL.\n *\n * The scanner skips string literals, quoted and bracketed identifiers, and both\n * comment forms, so a ':' inside them is text rather than a placeholder.\n *\n * No declare(strict_types=1) — see the note in host.php.\n */\n\nnamespace Atoms\\Cf;\n\nfinal class NamedParams\n{\n    private function __construct()\n    {\n    }\n\n    /**\n     * @param string $sql\n     * @param array<int|string, mixed> $bindings\n     * @return array{0: string, 1: list<mixed>} rewritten SQL and positional bindings\n     * @throws \\PDOException HY093 for a missing or unused named binding\n     */\n    public static function rewrite($sql, array $bindings)\n    {\n        $used = [];\n\n        if (self::isPositional($bindings)) {\n            if ($bindings === [] && strpos($sql, ':') !== false) {\n                // No bindings at all, but the statement may still carry a\n                // `:name`. SQLite binds unsupplied parameters to NULL without\n                // complaining, so this has to be caught here or it becomes a\n                // silently wrong answer. The strpos() guard keeps the common\n                // no-parameter statement off the scanner.\n                return self::scan($sql, [], $used);\n            }\n\n            // PDO's positional keys are 1-based and may arrive out of order\n            // (bindValue(2, ...) before bindValue(1, ...)), so order by key.\n            ksort($bindings, SORT_NUMERIC);\n\n            return [$sql, array_values($bindings)];\n        }\n\n        $lookup = [];\n        foreach ($bindings as $key => $value) {\n            $lookup[ltrim((string) $key, ':')] = $value;\n        }\n\n        list($rewritten, $positional) = self::scan($sql, $lookup, $used);\n\n        $unused = array_diff(array_keys($lookup), array_keys($used));\n        if ($unused !== []) {\n            throw new \\PDOException(sprintf(\n                'SQLSTATE[HY093] Invalid parameter number: binding(s) :%s not present in the statement.',\n                implode(', :', $unused)\n            ));\n        }\n\n        return [$rewritten, $positional];\n    }\n\n    /**\n     * Walk the statement, replacing `:name` outside literals and comments with\n     * `?` and collecting the matching values in order.\n     *\n     * @param string $sql\n     * @param array<string, mixed> $lookup binding values by bare name\n     * @param array<string, bool> $used out-parameter: names actually referenced\n     * @return array{0: string, 1: list<mixed>}\n     * @throws \\PDOException HY093 when a placeholder has no binding\n     */\n    private static function scan($sql, array $lookup, &$used)\n    {\n        $used = [];\n        $positional = [];\n        $out = '';\n        $length = strlen($sql);\n        $i = 0;\n\n        while ($i < $length) {\n            $char = $sql[$i];\n\n            if ($char === \"'\" || $char === '\"' || $char === '`') {\n                $end = self::skipQuoted($sql, $i, $char);\n                $out .= substr($sql, $i, $end - $i);\n                $i = $end;\n                continue;\n            }\n\n            if ($char === '[') {\n                $close = strpos($sql, ']', $i + 1);\n                $end = $close === false ? $length : $close + 1;\n                $out .= substr($sql, $i, $end - $i);\n                $i = $end;\n                continue;\n            }\n\n            if ($char === '-' && $i + 1 < $length && $sql[$i + 1] === '-') {\n                $close = strpos($sql, \"\\n\", $i);\n                $end = $close === false ? $length : $close + 1;\n                $out .= substr($sql, $i, $end - $i);\n                $i = $end;\n                continue;\n            }\n\n            if ($char === '/' && $i + 1 < $length && $sql[$i + 1] === '*') {\n                $close = strpos($sql, '*/', $i + 2);\n                $end = $close === false ? $length : $close + 2;\n                $out .= substr($sql, $i, $end - $i);\n                $i = $end;\n                continue;\n            }\n\n            if ($char === ':') {\n                // '::' is a cast operator, never a placeholder.\n                if ($i + 1 < $length && $sql[$i + 1] === ':') {\n                    $out .= '::';\n                    $i += 2;\n                    continue;\n                }\n\n                if (preg_match('/^:([A-Za-z_][A-Za-z0-9_]*)/', substr($sql, $i), $m) === 1) {\n                    $name = $m[1];\n\n                    if (!array_key_exists($name, $lookup)) {\n                        throw new \\PDOException(sprintf(\n                            'SQLSTATE[HY093] Invalid parameter number: no binding supplied for :%s.',\n                            $name\n                        ));\n                    }\n\n                    $positional[] = $lookup[$name];\n                    $used[$name] = true;\n                    $out .= '?';\n                    $i += strlen($m[0]);\n                    continue;\n                }\n            }\n\n            $out .= $char;\n            $i++;\n        }\n\n        return [$out, $positional];\n    }\n\n    /**\n     * @param array<int|string, mixed> $bindings\n     * @return bool\n     */\n    private static function isPositional(array $bindings)\n    {\n        foreach ($bindings as $key => $ignored) {\n            if (!is_int($key)) {\n                return false;\n            }\n        }\n\n        return true;\n    }\n\n    /**\n     * Index just past the closing quote of the literal starting at $start.\n     * SQL doubles the quote character to escape it ('' / \"\" / ``).\n     *\n     * @param string $sql\n     * @param int $start\n     * @param string $quote\n     * @return int\n     */\n    private static function skipQuoted($sql, $start, $quote)\n    {\n        $length = strlen($sql);\n        $i = $start + 1;\n\n        while ($i < $length) {\n            if ($sql[$i] !== $quote) {\n                $i++;\n                continue;\n            }\n\n            if ($i + 1 < $length && $sql[$i + 1] === $quote) {\n                $i += 2;\n                continue;\n            }\n\n            return $i + 1;\n        }\n\n        return $length;\n    }\n}\n",
  "/atoms/runtime/SqlBridge.php": "<?php\n\n/**\n * The single owner of the SQL seam: every statement and every transaction\n * boundary in the guest goes through exactly one of these.\n *\n * Both {@see BridgeDatabase} (the Atoms\\Database implementation) and\n * {@see AtomsPDO} / {@see AtomsStatement} (the leaky-but-real \\PDO surface)\n * hold the SAME instance, which is why `Database::transaction()`'s nesting\n * guard and `PDO::inTransaction()` observe one shared truth — exactly as they\n * do on the platform runtime, where both are views of one PDO connection.\n *\n * Statements run on `ctx.storage.sql` in the Durable Object. While a\n * transaction is open the host is parked inside\n * `ctx.storage.transactionSync(cb)`, so these same sync `sql.exec` calls land\n * inside the callback's scope (read-your-own-writes).\n *\n * No declare(strict_types=1) — see the note in host.php.\n */\n\nnamespace Atoms\\Cf;\n\nfinal class SqlBridge\n{\n    /** Row-returning execution mode for sql.exec. */\n    const MODE_ROWS = 'rows';\n\n    /** Counters-only execution mode for sql.exec. */\n    const MODE_RUN = 'run';\n\n    /** SQLSTATE reported when nothing has failed yet. */\n    const SQLSTATE_OK = '00000';\n\n    /** @var bool whether the host is currently parked inside transactionSync */\n    private $inTransaction = false;\n\n    /** @var int rowid of the most recent insert, 0 if there has been none */\n    private $lastInsertRowid = 0;\n\n    /** @var array{0: string, 1: int|null, 2: string|null} PDO-shaped errorInfo triple */\n    private $errorInfo = [self::SQLSTATE_OK, null, null];\n\n    /**\n     * Run one statement against the Durable Object's SQLite.\n     *\n     * @param string $sql\n     * @param list<mixed> $bindings positional only; named params are rewritten\n     *                              to positional by the caller, in PHP\n     * @param string $mode self::MODE_ROWS | self::MODE_RUN\n     * @return array{rows: list<array<string, mixed>>, rows_written: int, last_insert_rowid: int}\n     * @throws \\PDOException on a SQL error, with a real errorInfo() triple\n     */\n    public function exec($sql, array $bindings, $mode = self::MODE_ROWS)\n    {\n        $request = [\n            'op' => 'sql.exec',\n            'sql' => (string) $sql,\n            'bindings' => int64_encode($this->normalizeBindings($bindings)),\n            'mode' => $mode,\n        ];\n\n        $reply = host_sync_raw($request);\n\n        if ($reply['ok'] !== true) {\n            throw $this->failure($reply);\n        }\n\n        $this->errorInfo = [self::SQLSTATE_OK, null, null];\n\n        $rows = [];\n        if (isset($reply['rows']) && is_array($reply['rows'])) {\n            /** @var list<array<string, mixed>> $rows */\n            $rows = int64_decode($reply['rows']);\n        }\n\n        // The host reports `last_insert_rowid` only for a statement that actually\n        // wrote; the key is ABSENT for reads and for intercepted pragmas. That\n        // absence is meaningful and must not be read as \"0\": PDO's contract is\n        // that lastInsertId() keeps reporting the last insert across any number\n        // of intervening SELECTs, so the cached value is left alone here.\n        if (array_key_exists('last_insert_rowid', $reply)) {\n            $rowid = int64_decode($reply['last_insert_rowid']);\n            if (is_int($rowid)) {\n                $this->lastInsertRowid = $rowid;\n            }\n        }\n\n        return [\n            'rows' => $rows,\n            'rows_written' => isset($reply['rows_written']) ? (int) $reply['rows_written'] : 0,\n            'last_insert_rowid' => $this->lastInsertRowid,\n        ];\n    }\n\n    /**\n     * Open a transaction: the host enters `ctx.storage.transactionSync(cb)` and\n     * resumes the guest from inside `cb`.\n     *\n     * @throws \\PDOException when one is already open\n     */\n    public function begin()\n    {\n        if ($this->inTransaction) {\n            throw new \\PDOException('There is already an active transaction.');\n        }\n\n        host_park(['op' => 'tx.begin']);\n        $this->inTransaction = true;\n    }\n\n    /**\n     * Commit: park so the host's `cb` can return, which is what commits.\n     *\n     * @throws \\PDOException when none is open\n     */\n    public function commit()\n    {\n        $this->assertInTransaction('commit');\n\n        try {\n            host_park(['op' => 'tx.commit']);\n        } finally {\n            // Whatever happened, the guest is no longer inside cb. Leaving the\n            // flag set would wedge every later transaction.\n            $this->inTransaction = false;\n        }\n    }\n\n    /**\n     * Roll back: park so the host can throw its sentinel inside `cb`, which is\n     * what makes Cloudflare discard the write set.\n     *\n     * @throws \\PDOException when none is open\n     */\n    public function rollback()\n    {\n        $this->assertInTransaction('roll back');\n\n        try {\n            host_park(['op' => 'tx.rollback']);\n        } finally {\n            $this->inTransaction = false;\n        }\n    }\n\n    /**\n     * @return bool\n     */\n    public function inTransaction()\n    {\n        return $this->inTransaction;\n    }\n\n    /**\n     * PDO contract: a string, never an int.\n     *\n     * @return string\n     */\n    public function lastInsertId()\n    {\n        return (string) $this->lastInsertRowid;\n    }\n\n    /**\n     * @return string the SQLSTATE of the most recent statement\n     */\n    public function errorCode()\n    {\n        return $this->errorInfo[0];\n    }\n\n    /**\n     * @return array{0: string, 1: int|null, 2: string|null}\n     */\n    public function errorInfo()\n    {\n        return $this->errorInfo;\n    }\n\n    /**\n     * @param string $what\n     * @throws \\PDOException\n     */\n    private function assertInTransaction($what)\n    {\n        if (!$this->inTransaction) {\n            throw new \\PDOException(sprintf('There is no active transaction to %s.', $what));\n        }\n    }\n\n    /**\n     * Build the exception for an `ok: false` sql.exec reply and record the\n     * errorInfo triple that PDO consumers will read afterwards.\n     *\n     * @param array<string, mixed> $reply\n     * @return \\PDOException\n     */\n    private function failure(array $reply)\n    {\n        $error = isset($reply['error']) && is_array($reply['error']) ? $reply['error'] : [];\n        $code = isset($error['code']) ? (string) $error['code'] : 'sql_error';\n        $message = isset($error['message']) ? (string) $error['message'] : 'Unknown SQL error.';\n        $sqlstate = isset($error['sqlstate']) ? (string) $error['sqlstate'] : 'HY000';\n\n        // 1 == SQLITE_ERROR. The bridge has no finer driver code to report, so\n        // the triple stays shaped like PDO's without inventing detail.\n        $this->errorInfo = [$sqlstate, 1, $message];\n\n        $exception = new \\PDOException(sprintf('SQLSTATE[%s] [%s] %s', $sqlstate, $code, $message));\n        $exception->errorInfo = $this->errorInfo;\n\n        return $exception;\n    }\n\n    /**\n     * Coerce bindings into what the wire (and `ctx.storage.sql`) accepts.\n     *\n     * @param list<mixed> $bindings\n     * @return list<mixed>\n     * @throws \\PDOException on a value that cannot be bound\n     */\n    private function normalizeBindings(array $bindings)\n    {\n        $out = [];\n\n        foreach (array_values($bindings) as $index => $value) {\n            if ($value === null || is_int($value) || is_float($value) || is_string($value)) {\n                $out[] = $value;\n                continue;\n            }\n\n            if (is_bool($value)) {\n                // SQLite has no boolean type; PDO binds them as 0/1.\n                $out[] = $value ? 1 : 0;\n                continue;\n            }\n\n            throw new \\PDOException(sprintf(\n                'SQLSTATE[HY105] Invalid parameter type: binding %d is a %s; '\n                . 'only null, bool, int, float and string cross the Atoms SQL bridge.',\n                $index,\n                is_object($value) ? get_class($value) : gettype($value)\n            ));\n        }\n\n        return $out;\n    }\n}\n",
  "/atoms/runtime/bootstrap.php": "<?php\n\n/**\n * The guest entry point: one activation, then the parked turn loop.\n *\n * `php.run()` is a REQUEST — classes, constants and globals are torn down when\n * it returns — established by the pre-MVP spike. An Atom whose state lives in PHP\n * memory therefore cannot be one `php.run()` per turn. Everything below happens\n * inside ONE `php.run()` that does not return until the host asks for shutdown:\n * the guest parks itself on the `turn.await` door between turns and the host\n * resumes it with the next envelope, so the PHP stack — and with it the Atom\n * object and all its in-memory state — is never unwound. Shape ported from\n * the spike's loop.\n *\n * Load order, and the composed entry script the JS host must run, are\n * documented in ../README.md.\n *\n * No declare(strict_types=1) — a declare() must be the very first statement of\n * a file and this one is `require`d from a host-composed script; the spike hit\n * hard fatals on exactly this. The verbatim atoms-core files\n * keep their own declare() because they are only ever `require`d, one file each.\n */\n\nnamespace Atoms\\Cf;\n\nuse Atoms\\Atom;\nuse Atoms\\Migrations\\MigrationSet;\nuse Atoms\\Migrations\\Migrator;\nuse Atoms\\Runtime\\LifecycleInvoker;\nuse Atoms\\Serialization\\Serializer;\n\n/** Where the host writes this prelude, unless $CFG['paths']['runtime'] says otherwise. */\nconst RUNTIME_DIR_DEFAULT = '/atoms/runtime';\n\n/** Where the host writes the verbatim atoms/core sources, unless $CFG overrides it. */\nconst CORE_DIR_DEFAULT = '/atoms/core/src';\n\n/**\n * The atoms/core files, in dependency order. Verbatim copies — this list is the\n * runtime's own, not something the bundle gets to choose.\n *\n * @return list<string> paths relative to the core source directory\n */\nfunction core_files()\n{\n    return [\n        'Errors/ErrorCode.php',\n        'Errors/CatalogEntry.php',\n        'Errors/ErrorCatalog.php',\n        'Errors/AtomsError.php',\n        'Attributes/MethodsFor.php',\n        'Attributes/SharedWithAtoms.php',\n        'Serialization/Payload.php',\n        'Serialization/SerializationException.php',\n        'Serialization/Serializer.php',\n        'Database.php',\n        'AtomJob.php',\n        'AtomMethods.php',\n        'Websocket/Connection.php',\n        'Websocket/Message.php',\n        'Timers/Timers.php',\n        'Runtime/AtomContext.php',\n        'Atom.php',\n        'Runtime/LifecycleInvoker.php',\n        'Migrations/Migration.php',\n        'Migrations/MigrationEntry.php',\n        'Migrations/MigrationSet.php',\n        'Migrations/Migrator.php',\n    ];\n}\n\n/**\n * The rest of the Atoms\\Cf prelude, in dependency order. host.php and int64.php\n * are required by hand below, before anything can fail usefully.\n *\n * @return list<string> paths relative to the runtime directory\n */\nfunction runtime_files()\n{\n    return [\n        'BootstrapError.php',\n        'MigrationsGlobShim.php',\n        'AtomsNotSupported.php',\n        'FetchMode.php',\n        'NamedParams.php',\n        'SqlBridge.php',\n        'AtomsStatement.php',\n        'AtomsPDO.php',\n        'BridgeDatabase.php',\n        'CfAtomContext.php',\n    ];\n}\n\n/**\n * Plain \\RuntimeException rather than BootstrapError: this runs before (and in\n * order to load) the class that would otherwise report it.\n *\n * @param string $dir\n * @param list<string> $files\n * @throws \\RuntimeException when a file the runtime owns is missing from MEMFS\n */\nfunction require_all($dir, array $files)\n{\n    foreach ($files as $relative) {\n        $path = rtrim($dir, '/') . '/' . $relative;\n\n        if (!is_file($path)) {\n            throw new \\RuntimeException(\n                sprintf('Atoms runtime file %s was not written into the guest filesystem.', $path)\n            );\n        }\n\n        require_once $path;\n    }\n}\n\n/**\n * Index the bundle's PHP files by declared symbol and register an autoloader\n * for them, so a customer Atom can reference Payload DTOs, enums and helper\n * classes that live in other files without the runtime imposing a load order.\n *\n * Best effort by design (bundle_format 0 carries no class map): declarations\n * are recognised at the start of a line, which covers every named top-level\n * class, interface, trait and enum. Anonymous classes and conditionally\n * declared ones are not indexed — they cannot be autoloaded anyway.\n *\n * @param list<string> $files guest paths the host wrote into MEMFS\n * @param list<string> $exclude paths that must never be autoloaded (migrations)\n * @return int the number of indexed symbols\n */\nfunction register_bundle_autoloader(array $files, array $exclude)\n{\n    $skip = array_flip($exclude);\n    $index = [];\n\n    foreach ($files as $path) {\n        if (substr($path, -4) !== '.php' || isset($skip[$path]) || !is_file($path)) {\n            continue;\n        }\n\n        $source = file_get_contents($path);\n        if ($source === false) {\n            continue;\n        }\n\n        $namespace = '';\n        if (preg_match('/^[ \\t]*namespace[ \\t]+([A-Za-z_\\x80-\\xff][A-Za-z0-9_\\x80-\\xff\\\\\\\\]*)[ \\t]*[;{]/m', $source, $m) === 1) {\n            $namespace = $m[1] . '\\\\';\n        }\n\n        if (preg_match_all(\n            '/^[ \\t]*(?:(?:final|abstract|readonly)[ \\t]+)*(?:class|interface|trait|enum)[ \\t]+([A-Za-z_\\x80-\\xff][A-Za-z0-9_\\x80-\\xff]*)/m',\n            $source,\n            $matches\n        ) > 0) {\n            foreach ($matches[1] as $name) {\n                $index[strtolower($namespace . $name)] = $path;\n            }\n        }\n    }\n\n    spl_autoload_register(function ($class) use ($index) {\n        $key = strtolower(ltrim((string) $class, '\\\\'));\n\n        if (isset($index[$key])) {\n            require_once $index[$key];\n        }\n    });\n\n    return count($index);\n}\n\n/**\n * Apply this Atom type's pending migrations with the real\n * `Atoms\\Migrations\\Migrator`, which runs each one in its own transaction and\n * tracks progress in `PRAGMA user_version` — a pragma the host intercepts and\n * maps onto `__atoms_meta` (mvp-spec.md §SQL bridge details), so the unmodified\n * Migrator works over the bridge.\n *\n * @param BridgeDatabase $db\n * @param list<string> $paths migration files, from the manifest\n * @param string $type\n * @return int migrations applied\n * @throws BootstrapError\n */\nfunction apply_migrations(BridgeDatabase $db, array $paths, $type)\n{\n    if ($paths === []) {\n        return 0;\n    }\n\n    $directories = [];\n    foreach ($paths as $path) {\n        if (!is_file($path)) {\n            throw new BootstrapError(\n                'internal',\n                sprintf('Migration %s for atom type %s is missing from the guest filesystem.', $path, $type)\n            );\n        }\n\n        $directories[dirname($path)] = true;\n    }\n\n    if (count($directories) !== 1) {\n        throw new BootstrapError(\n            'internal',\n            sprintf(\n                'Atom type %s has migrations in %d directories; MigrationSet::fromDirectory() scans exactly one.',\n                $type,\n                count($directories)\n            )\n        );\n    }\n\n    $directory = (string) array_key_first($directories);\n    $set = MigrationSet::fromDirectory($directory);\n\n    // MigrationSet globs the directory. If the guest filesystem ever answers a\n    // glob differently from what the manifest promised, migrations would\n    // silently not apply — which is exactly the failure this runtime must never\n    // have. Fail the activation loudly instead.\n    if (count($set) !== count($paths)) {\n        throw new BootstrapError(\n            'internal',\n            sprintf(\n                'Migration discovery mismatch for atom type %s in %s: the manifest lists %d file(s), '\n                . 'MigrationSet found %d.',\n                $type,\n                $directory,\n                count($paths),\n                count($set)\n            )\n        );\n    }\n\n    $migrator = new Migrator();\n\n    return $migrator->apply($db, $set);\n}\n\n/**\n * Resolve an invocable Atom method, refusing anything that is not a customer\n * method: private/protected/static/abstract members, magic methods, and the\n * base class's own surface (the constructor and the WebSocket handlers, which\n * are out of scope for the MVP).\n *\n * @param object $atom\n * @param mixed $method\n * @return \\ReflectionMethod\n * @throws BootstrapError with code 'method_not_found'\n */\nfunction invocable_method($atom, $method)\n{\n    $class = get_class($atom);\n\n    if (!is_string($method) || preg_match('/^[A-Za-z_][A-Za-z0-9_]*$/', $method) !== 1) {\n        throw new BootstrapError('method_not_found', sprintf('%s has no method %s.', $class, var_export($method, true)));\n    }\n\n    if (!method_exists($atom, $method)) {\n        throw new BootstrapError('method_not_found', sprintf('%s has no method %s().', $class, $method));\n    }\n\n    $reflection = new \\ReflectionMethod($atom, $method);\n\n    if (!$reflection->isPublic() || $reflection->isStatic() || $reflection->isAbstract()) {\n        throw new BootstrapError('method_not_found', sprintf('%s::%s() is not an invocable Atom method.', $class, $method));\n    }\n\n    if (strncmp($reflection->getName(), '__', 2) === 0) {\n        throw new BootstrapError('method_not_found', sprintf('%s::%s() is a magic method.', $class, $method));\n    }\n\n    if ($reflection->getDeclaringClass()->getName() === Atom::class) {\n        throw new BootstrapError(\n            'method_not_found',\n            sprintf('%s::%s() is part of the Atom base class, not of %s.', Atom::class, $method, $class)\n        );\n    }\n\n    return $reflection;\n}\n\n/**\n * The spec's turn-result error envelope. Traces never appear here — they go to\n * the host's structured log instead (mvp-spec.md §Turn-result envelope).\n *\n * @param string $code\n * @param string $message\n * @param string|null $class FQCN of the throwable, null when there was none\n * @return array<string, mixed>\n */\nfunction error_envelope($code, $message, $class)\n{\n    return [\n        'ok' => false,\n        'error' => [\n            'code' => $code,\n            'message' => $message,\n            'class' => $class,\n        ],\n    ];\n}\n\n/**\n * Force a string into printable ASCII.\n *\n * Exception messages reach the wire, and `json_encode()` fails outright on\n * invalid UTF-8 — a customer throwing `new \\RuntimeException(\"\\xB1\")` would\n * otherwise make the turn result unencodable and take the residency with it.\n *\n * @param mixed $text\n * @param int $limit\n * @return string\n */\nfunction ascii_excerpt($text, $limit = 500)\n{\n    $out = preg_replace('/[^\\x20-\\x7E]/', '?', (string) $text);\n\n    return strlen((string) $out) > $limit ? substr((string) $out, 0, $limit) . '...' : (string) $out;\n}\n\n/**\n * Guarantee that a turn-result envelope can actually be parked.\n *\n * `turn_loop()` hands the envelope to `host_park()`, which `json_encode()`s it.\n * Encoding fails on invalid UTF-8, on a recursive structure, and — the case\n * that matters most — on anything nested past json_encode()'s depth limit, all\n * of which a customer method can return. An unencodable envelope must not\n * become a \\RuntimeException thrown out of the loop: that unwinds php.run() and\n * poisons the residency for a plain application bug. The whole park request is\n * trialled here, so the depth budget checked is exactly the one the real call\n * will use.\n *\n * @param array<string, mixed> $envelope\n * @param mixed $method\n * @return array<string, mixed> the envelope, or an encodable error envelope\n */\nfunction encodable_envelope(array $envelope, $method)\n{\n    if (json_encode(['op' => 'turn.await', 'result' => $envelope], JSON_UNESCAPED_SLASHES) !== false) {\n        return $envelope;\n    }\n\n    $why = json_last_error_msg();\n\n    host_log('error', [\n        'event' => 'turn_result_unencodable',\n        'method' => is_string($method) ? $method : '',\n        'reason' => $why,\n    ]);\n\n    // Deliberately built from ASCII literals only, so this one cannot fail too.\n    return error_envelope(\n        'atom_exception',\n        sprintf(\n            'The value returned by %s() cannot cross the Atoms bridge: %s. '\n            . 'Return data that is JSON-encodable, UTF-8 clean and not deeply nested.',\n            ascii_excerpt(is_string($method) ? $method : '?', 100),\n            ascii_excerpt($why, 200)\n        ),\n        null\n    );\n}\n\n/**\n * Settle a transaction the turn left open.\n *\n * The host is still parked inside `ctx.storage.transactionSync(cb)` at this\n * point, and it refuses every park op but tx.commit/tx.rollback while it is —\n * so parking at `turn.await` here would strand the guest. Forgetting\n * `commit()`, or catching an exception above the frame that opened the\n * transaction, is an ordinary application bug: it must produce a typed\n * `atom_exception` and leave the Atom serving turns, not a destroyed residency\n * that every retry destroys again. The write set is discarded, because a turn\n * that never committed must not appear to have.\n *\n * @param SqlBridge $bridge\n * @param array<string, mixed> $identity\n * @param mixed $method\n * @return string|null null when nothing was open, else what to report\n */\nfunction settle_open_transaction(SqlBridge $bridge, array $identity, $method)\n{\n    if (!$bridge->inTransaction()) {\n        return null;\n    }\n\n    $name = ascii_excerpt(is_string($method) ? $method : '?', 100);\n    $detail = '';\n\n    try {\n        $bridge->rollback();\n    } catch (\\Throwable $e) {\n        // SqlBridge::rollback() clears its own flag in a finally, so the next\n        // turn still starts from a known state.\n        $detail = ' The rollback itself failed: ' . ascii_excerpt($e->getMessage(), 200) . '.';\n    }\n\n    host_log('error', [\n        'event' => 'transaction_left_open',\n        'atom_type' => $identity['type'],\n        'atom_id' => $identity['id'],\n        'method' => $name,\n        'message' => 'the turn ended inside an open transaction; it was rolled back',\n    ]);\n\n    return sprintf(\n        '%s() returned with a database transaction still open. It was rolled back, so nothing '\n        . 'it wrote was kept. Every transaction must be committed or rolled back within the turn '\n        . 'that opened it; prefer $this->db()->transaction(...), which does that for you.%s',\n        $name,\n        $detail\n    );\n}\n\n/**\n * Run one turn and produce its result envelope. Never throws: a turn's failure\n * is data the host reports, not a reason to poison the residency.\n *\n * @param object $atom\n * @param Serializer $serializer\n * @param SqlBridge $bridge the shared SQL seam, so an abandoned transaction can\n *                          be settled before the turn boundary is reached\n * @param array<string, mixed> $identity {type, id}\n * @param mixed $method\n * @param list<mixed> $args still int64-TAGGED; decoded below, inside the guard\n * @return array<string, mixed>\n */\nfunction run_turn($atom, Serializer $serializer, SqlBridge $bridge, array $identity, $method, array $args)\n{\n    try {\n        // Decoding happens INSIDE the guard on purpose. The args come from the\n        // client, so a malformed or out-of-range {\"$atoms_int64\":...} tag is\n        // attacker-controlled input; decoding it in turn_loop() would let the\n        // RuntimeException escape the loop, unwind php.run() and poison the\n        // whole residency. The host rejects such a tag with `int64_range`\n        // before it ever gets here — this is the second line of defence.\n        $args = int64_decode($args);\n\n        try {\n            $reflection = invocable_method($atom, $method);\n        } catch (BootstrapError $e) {\n            return error_envelope('method_not_found', $e->getMessage(), null);\n        }\n\n        try {\n            $callArgs = $serializer->denormalizeArguments($args, $reflection);\n            $value = $reflection->invokeArgs($atom, $callArgs);\n            $normalized = $serializer->normalize($value);\n        } catch (\\Throwable $e) {\n            host_log('error', [\n                'event' => 'turn_failed',\n                'atom_type' => $identity['type'],\n                'atom_id' => $identity['id'],\n                'method' => (string) $method,\n                'class' => get_class($e),\n                'message' => $e->getMessage(),\n                'trace' => $e->getTraceAsString(),\n            ]);\n\n            // A throw that escaped the customer's own transaction handling can\n            // leave one open; settle it before the turn boundary. The original\n            // throwable is still what the caller is told about.\n            settle_open_transaction($bridge, $identity, $method);\n\n            return error_envelope('atom_exception', $e->getMessage(), get_class($e));\n        }\n\n        $leaked = settle_open_transaction($bridge, $identity, $method);\n        if ($leaked !== null) {\n            return error_envelope('atom_exception', $leaked, null);\n        }\n\n        return ['ok' => true, 'result' => int64_encode($normalized)];\n    } catch (\\Throwable $e) {\n        // Anything that escapes the inner handlers is a runtime bug, not the\n        // customer's: envelope construction, int64 tagging, reflection.\n        host_log('error', [\n            'event' => 'turn_internal_error',\n            'atom_type' => $identity['type'],\n            'atom_id' => $identity['id'],\n            'method' => is_string($method) ? $method : '',\n            'class' => get_class($e),\n            'message' => $e->getMessage(),\n            'trace' => $e->getTraceAsString(),\n        ]);\n\n        return error_envelope('internal', $e->getMessage(), get_class($e));\n    }\n}\n\n/**\n * Park between turns forever. The `result` field carries the PREVIOUS turn's\n * envelope and is null on the first park after boot (mvp-spec.md §Park ops).\n *\n * @param object $atom\n * @param SqlBridge $bridge\n * @param array<string, mixed> $identity\n */\nfunction turn_loop($atom, SqlBridge $bridge, array $identity)\n{\n    $serializer = new Serializer();\n    $result = null;\n\n    while (true) {\n        // The park is guarded, and this is the load-bearing guard of the whole\n        // runtime. `host_park()` throws a \\RuntimeException on three paths that\n        // client-controlled data reaches:\n        //\n        //   - the OUTBOUND json_encode() of the park request fails (a customer\n        //     method returned something unencodable — mostly covered by\n        //     encodable_envelope() below, this is the backstop);\n        //   - the INBOUND json_decode() of the turn envelope fails, which is\n        //     what a request whose args nest past PHP's json_decode() depth\n        //     limit produces;\n        //   - the host answers ok:false.\n        //\n        // Outside a try/catch, any of those unwinds turn_loop() -> activate()\n        // -> php.run(), the host sees no park, and the residency is poisoned and\n        // cold-booted — one cheap request could kill any Atom, repeatably. In\n        // here it is just a failed turn: report it and stay parked.\n        try {\n            $envelope = host_park(['op' => 'turn.await', 'result' => $result]);\n        } catch (\\Throwable $e) {\n            host_log('error', [\n                'event' => 'turn_park_failed',\n                'atom_type' => $identity['type'],\n                'atom_id' => $identity['id'],\n                'class' => get_class($e),\n                'message' => $e->getMessage(),\n            ]);\n\n            // Fixed ASCII shape, so parking THIS result cannot fail in turn.\n            $result = error_envelope(\n                'internal',\n                'The Atoms turn boundary could not be crossed: ' . ascii_excerpt($e->getMessage()),\n                get_class($e)\n            );\n            continue;\n        }\n\n        $kind = isset($envelope['kind']) ? $envelope['kind'] : null;\n\n        if ($kind === 'shutdown') {\n            return;\n        }\n\n        if ($kind !== 'invoke') {\n            // A host-side protocol bug. Answer it and stay resident rather than\n            // taking the whole residency down.\n            $result = error_envelope(\n                'internal',\n                sprintf('turn.await resumed with unknown envelope kind %s.', var_export($kind, true)),\n                null\n            );\n            continue;\n        }\n\n        $args = isset($envelope['args']) && is_array($envelope['args']) ? array_values($envelope['args']) : [];\n        $method = isset($envelope['method']) ? $envelope['method'] : null;\n\n        $result = encodable_envelope(\n            run_turn($atom, $serializer, $bridge, $identity, $method, $args),\n            $method\n        );\n    }\n}\n\n/**\n * Activation: load everything, migrate, construct the Atom, fire onActivation,\n * then hand over to the turn loop.\n *\n * @param array<string, mixed> $cfg\n * @throws BootstrapError\n */\nfunction activate(array $cfg)\n{\n    $runtimeDir = isset($cfg['paths']['runtime']) ? (string) $cfg['paths']['runtime'] : RUNTIME_DIR_DEFAULT;\n    $coreDir = isset($cfg['paths']['core']) ? (string) $cfg['paths']['core'] : CORE_DIR_DEFAULT;\n\n    // atoms-core first: BridgeDatabase implements Atoms\\Database and\n    // CfAtomContext implements Atoms\\Runtime\\AtomContext, so those interfaces\n    // must exist before the prelude classes are declared.\n    require_all($coreDir, core_files());\n    require_all($runtimeDir, runtime_files());\n\n    if (!isset($cfg['atom']['type']) || !is_string($cfg['atom']['type']) || $cfg['atom']['type'] === '') {\n        throw new BootstrapError('internal', '$CFG.atom.type is missing.');\n    }\n    if (!isset($cfg['atom']['id']) || !is_string($cfg['atom']['id'])) {\n        throw new BootstrapError('internal', '$CFG.atom.id is missing.');\n    }\n\n    $type = $cfg['atom']['type'];\n    $id = $cfg['atom']['id'];\n    $identity = ['type' => $type, 'id' => $id];\n\n    $atoms = isset($cfg['manifest']['atoms']) && is_array($cfg['manifest']['atoms']) ? $cfg['manifest']['atoms'] : [];\n\n    if (!isset($atoms[$type]) || !is_array($atoms[$type])) {\n        throw new BootstrapError(\n            'atom_not_found',\n            sprintf('Atom type %s is not in the deployed bundle manifest.', $type)\n        );\n    }\n\n    $entry = $atoms[$type];\n\n    foreach (['class', 'file'] as $required) {\n        if (!isset($entry[$required]) || !is_string($entry[$required]) || $entry[$required] === '') {\n            throw new BootstrapError(\n                'internal',\n                sprintf('Manifest entry for atom type %s is missing \"%s\".', $type, $required)\n            );\n        }\n    }\n\n    // Migration files are required by MigrationEntry at apply time and must\n    // never be reachable through the class autoloader.\n    $migrationPaths = [];\n    foreach ($atoms as $each) {\n        if (isset($each['migrations']) && is_array($each['migrations'])) {\n            foreach ($each['migrations'] as $path) {\n                $migrationPaths[] = (string) $path;\n            }\n        }\n    }\n\n    $bundleFiles = isset($cfg['files']) && is_array($cfg['files']) ? array_values($cfg['files']) : [$entry['file']];\n    register_bundle_autoloader($bundleFiles, $migrationPaths);\n\n    $bridge = new SqlBridge();\n    $db = new BridgeDatabase($bridge);\n    $context = new CfAtomContext($db);\n\n    $ownMigrations = isset($entry['migrations']) && is_array($entry['migrations'])\n        ? array_map('strval', array_values($entry['migrations']))\n        : [];\n\n    $applied = apply_migrations($db, $ownMigrations, $type);\n\n    if (!is_file($entry['file'])) {\n        throw new BootstrapError(\n            'internal',\n            sprintf('Atom source %s for type %s is missing from the guest filesystem.', $entry['file'], $type)\n        );\n    }\n\n    require_once $entry['file'];\n\n    $class = $entry['class'];\n\n    if (!class_exists($class)) {\n        throw new BootstrapError(\n            'internal',\n            sprintf('Atom class %s was not declared by %s.', $class, $entry['file'])\n        );\n    }\n\n    if (!is_subclass_of($class, Atom::class)) {\n        throw new BootstrapError(\n            'internal',\n            sprintf('Atom class %s does not extend %s.', $class, Atom::class)\n        );\n    }\n\n    $atom = new $class($id, $context);\n\n    LifecycleInvoker::activate($atom);\n\n    // Same reasoning as in run_turn(): the first park is turn.await, which the\n    // host refuses while a transaction is open. onActivation() is best-effort by\n    // contract, so an abandoned one is rolled back and logged rather than made\n    // to fail the activation — failing it would cold-boot the residency on every\n    // request, forever, for a deterministic customer bug.\n    settle_open_transaction($bridge, $identity, 'onActivation');\n\n    host_log('info', [\n        'event' => 'activated',\n        'atom_type' => $type,\n        'atom_id' => $id,\n        'class' => $class,\n        'migrations_applied' => $applied,\n        'php_version' => PHP_VERSION,\n    ]);\n\n    turn_loop($atom, $bridge, $identity);\n}\n\n// ---------------------------------------------------------------------------\n// Entry. The host's composed script defines $CFG and requires this file.\n// ---------------------------------------------------------------------------\n\n$__atoms_runtime_dir = isset($CFG['paths']['runtime']) ? (string) $CFG['paths']['runtime'] : RUNTIME_DIR_DEFAULT;\n\nrequire_once rtrim($__atoms_runtime_dir, '/') . '/host.php';\nrequire_once rtrim($__atoms_runtime_dir, '/') . '/int64.php';\n\ntry {\n    if (!isset($CFG) || !is_array($CFG)) {\n        throw new \\RuntimeException('Atoms: the host did not define $CFG before requiring bootstrap.php.');\n    }\n\n    activate($CFG);\n} catch (\\Throwable $__atoms_boot_error) {\n    // No turn-result envelope exists yet, so the failure is reported on the log\n    // door (with its trace) and then rethrown: php.run() unwinds, and the host\n    // treats the residency as poisoned per mvp-spec.md §AtomDurableObject\n    // lifecycle.\n    host_log('error', [\n        'event' => 'activation_failed',\n        'code' => $__atoms_boot_error instanceof BootstrapError ? $__atoms_boot_error->atomsCode() : 'internal',\n        'atom_type' => isset($CFG['atom']['type']) ? (string) $CFG['atom']['type'] : null,\n        'atom_id' => isset($CFG['atom']['id']) ? (string) $CFG['atom']['id'] : null,\n        'class' => get_class($__atoms_boot_error),\n        'message' => $__atoms_boot_error->getMessage(),\n        'trace' => $__atoms_boot_error->getTraceAsString(),\n    ]);\n\n    throw $__atoms_boot_error;\n}\n",
  "/atoms/runtime/host.php": "<?php\n\n/**\n * The two doors between the PHP guest and the JS host.\n *\n * There is exactly ONE wasm import (php-wasm's `post_message_to_js`); the host\n * dispatches on the first byte of the message string:\n *\n *   '!'  SYNC  — the JS handler answers synchronously, writing the reply into\n *               guest memory. No Asyncify unwind. Used for sql.exec, config.get\n *               and log.\n *   '~'  PARK  — the JS handler receives the message plus a `reply(str)`\n *               callback and decides *when* and *from which JS stack frame* the\n *               guest resumes. Used for turn.await and the tx.* ops (resuming\n *               inside `ctx.storage.transactionSync(cb)` is the whole reason\n *               this build is Asyncify and not JSPI).\n *\n * Wire shape (mvp-spec.md §PHP↔JS protocol): the request is a JSON object with\n * an `op`; every reply is a JSON object carrying either `ok: true` (plus\n * op-specific fields) or `ok: false, error: {code, message}`.\n *\n * NOTE: no `declare(strict_types=1)` anywhere under runtime/. A declare() must\n * be the very first statement of a file, and these files may be composed by the\n * host; the pre-MVP spike hit hard fatals on exactly this.\n * The verbatim atoms-core files keep their own declare() and are therefore only\n * ever `require`d, never concatenated.\n */\n\nnamespace Atoms\\Cf;\n\n/** First byte selecting the synchronous door. */\nconst DOOR_SYNC = '!';\n\n/** First byte selecting the parking (Asyncify-suspending) door. */\nconst DOOR_PARK = '~';\n\n/** How much of a malformed reply to quote back in an exception message. */\nconst HOST_ERROR_EXCERPT = 400;\n\n/**\n * Raw door call: encode, cross, decode, and validate only that the reply is a\n * well-formed envelope. `ok: false` is returned to the caller, not thrown.\n *\n * @param string $door one of DOOR_SYNC / DOOR_PARK\n * @param array<string, mixed> $req\n * @return array<string, mixed> the decoded envelope (has an `ok` key)\n * @throws \\RuntimeException when the request cannot be encoded or the reply is\n *                           not a JSON object with an `ok` key\n */\nfunction host_call($door, array $req)\n{\n    $encoded = json_encode($req, JSON_UNESCAPED_SLASHES);\n    if ($encoded === false) {\n        // Most often a binary/invalid-UTF-8 SQL binding. Corrupting it silently\n        // (JSON_INVALID_UTF8_SUBSTITUTE) would be worse than failing.\n        throw new \\RuntimeException(sprintf(\n            'Atoms: could not encode host request for op \"%s\": %s. '\n            . 'Binary values do not cross the MVP bridge; store them base64-encoded.',\n            isset($req['op']) ? (string) $req['op'] : '?',\n            json_last_error_msg()\n        ));\n    }\n\n    $reply = \\post_message_to_js($door . $encoded);\n\n    $out = json_decode((string) $reply, true);\n    if (!is_array($out) || !array_key_exists('ok', $out)) {\n        throw new \\RuntimeException(sprintf(\n            'Atoms: malformed host reply for op \"%s\" on door \"%s\": %s',\n            isset($req['op']) ? (string) $req['op'] : '?',\n            $door,\n            substr((string) $reply, 0, HOST_ERROR_EXCERPT)\n        ));\n    }\n\n    return $out;\n}\n\n/**\n * Synchronous host call that throws on `ok: false`.\n *\n * @param array<string, mixed> $req\n * @return array<string, mixed>\n * @throws \\RuntimeException\n */\nfunction host_sync(array $req)\n{\n    return host_checked(host_call(DOOR_SYNC, $req), $req);\n}\n\n/**\n * Synchronous host call that hands the raw envelope back, `ok: false` included.\n * Used by the SQL path, which maps `sql_error` onto a \\PDOException carrying a\n * real errorInfo() triple rather than a bare \\RuntimeException.\n *\n * @param array<string, mixed> $req\n * @return array<string, mixed>\n */\nfunction host_sync_raw(array $req)\n{\n    return host_call(DOOR_SYNC, $req);\n}\n\n/**\n * Parking host call that throws on `ok: false`. The guest stack is suspended\n * (Asyncify) until the host invokes its resume callback.\n *\n * @param array<string, mixed> $req\n * @return array<string, mixed>\n * @throws \\RuntimeException\n */\nfunction host_park(array $req)\n{\n    return host_checked(host_call(DOOR_PARK, $req), $req);\n}\n\n/**\n * Turn an `ok: false` envelope into an exception; pass `ok: true` through.\n *\n * @param array<string, mixed> $reply\n * @param array<string, mixed> $req\n * @return array<string, mixed>\n * @throws \\RuntimeException\n */\nfunction host_checked(array $reply, array $req)\n{\n    if ($reply['ok'] === true) {\n        return $reply;\n    }\n\n    $error = isset($reply['error']) && is_array($reply['error']) ? $reply['error'] : [];\n\n    throw new \\RuntimeException(sprintf(\n        'Atoms host op \"%s\" failed [%s]: %s',\n        isset($req['op']) ? (string) $req['op'] : '?',\n        isset($error['code']) ? (string) $error['code'] : 'unknown',\n        isset($error['message']) ? (string) $error['message'] : 'no message'\n    ));\n}\n\n/**\n * Structured server-side log line. Best effort: logging must never be the reason\n * a turn fails, so transport problems are swallowed here (and only here).\n *\n * @param string $level\n * @param array<string, mixed> $fields\n */\nfunction host_log($level, array $fields)\n{\n    try {\n        host_call(DOOR_SYNC, ['op' => 'log', 'level' => (string) $level, 'fields' => $fields]);\n    } catch (\\Throwable $ignored) {\n        // Deliberately swallowed — see above.\n    }\n}\n",
  "/atoms/runtime/int64.php": "<?php\n\n/**\n * The int64 tagging codec, PHP side (mvp-spec.md §Int64 tagging).\n *\n * JSON numbers are exact only through 2^53−1. Any integer outside\n * ±(2^53−1) crosses the PHP↔JS boundary as\n *\n *     {\"$atoms_int64\": \"<decimal string>\"}\n *\n * in SQL bindings, result rows, `last_insert_rowid`, method args and method\n * results. JS holds those as BigInt; PHP decodes them to native ints (this\n * php-wasm build is 64-bit-int capable). A tagged value outside int64 range is\n * an error, never a truncation.\n *\n * No declare(strict_types=1) — see the note in host.php.\n */\n\nnamespace Atoms\\Cf;\n\n/** The wire key that marks a tagged 64-bit integer. */\nconst INT64_TAG = '$atoms_int64';\n\n/** 2^53−1: the largest integer JSON can carry losslessly. */\nconst JSON_SAFE_INT = 9007199254740991;\n\n/**\n * Recursively replace out-of-JSON-range ints with their tagged form.\n *\n * @param mixed $value\n * @return mixed\n */\nfunction int64_encode($value)\n{\n    if (is_int($value)) {\n        if ($value > JSON_SAFE_INT || $value < -JSON_SAFE_INT) {\n            return [INT64_TAG => (string) $value];\n        }\n\n        return $value;\n    }\n\n    if (is_array($value)) {\n        $out = [];\n        foreach ($value as $key => $item) {\n            $out[$key] = int64_encode($item);\n        }\n\n        return $out;\n    }\n\n    return $value;\n}\n\n/**\n * Recursively replace tagged integers with native ints.\n *\n * @param mixed $value\n * @return mixed\n * @throws \\RuntimeException when a tag payload is not a canonical decimal\n *                           integer inside int64 range\n */\nfunction int64_decode($value)\n{\n    if (!is_array($value)) {\n        return $value;\n    }\n\n    if (count($value) === 1 && array_key_exists(INT64_TAG, $value)) {\n        return int64_parse($value[INT64_TAG]);\n    }\n\n    $out = [];\n    foreach ($value as $key => $item) {\n        $out[$key] = int64_decode($item);\n    }\n\n    return $out;\n}\n\n/**\n * Parse a tag payload into a native int, refusing anything lossy.\n *\n * @param mixed $decimal\n * @return int\n * @throws \\RuntimeException\n */\nfunction int64_parse($decimal)\n{\n    if (!is_string($decimal) || !preg_match('/^-?(0|[1-9][0-9]*)$/', $decimal)) {\n        throw new \\RuntimeException(sprintf(\n            'Atoms: %s payload must be a canonical decimal string, got %s.',\n            INT64_TAG,\n            var_export($decimal, true)\n        ));\n    }\n\n    $native = (int) $decimal;\n\n    // (int) saturates at PHP_INT_MIN/PHP_INT_MAX instead of overflowing, so a\n    // round-trip comparison is an exact range check.\n    if ((string) $native !== $decimal) {\n        throw new \\RuntimeException(sprintf(\n            'Atoms: %s payload \"%s\" is outside the signed 64-bit range.',\n            INT64_TAG,\n            $decimal\n        ));\n    }\n\n    return $native;\n}\n"
},
};
